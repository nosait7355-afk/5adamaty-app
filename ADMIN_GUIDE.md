# دليل لوحة الإدارة — خدماتي الفيوم

لوحة الإدارة على `/admin` — لا يصل إليها إلا حساب بدور `ADMIN`. أي محاولة وصول بحساب آخر تُعاد توجيهها تلقائيًا.

## لوحة المعلومات — `/admin/dashboard`

نظرة عامة: عدد مقدمي الخدمة والعملاء، طلبات التوثيق المعلَّقة، المهن والتصنيفات النشطة، متوسط التقييم، وعدد الطلبات المكتملة مع **مجموع قيمتها كتقرير إحصائي بحت** — لا يمثّل معاملة مالية داخل التطبيق؛ الدفع دائمًا نقدًا مباشرة بين الطرفين خارج المنصة.

## توثيق مقدمي الخدمة — `/admin/providers`

مراجعة طلبات انضمام مقدمي الخدمة: البيانات، المستندات المرفوعة (تُفتح برابط موقّت ينتهي خلال دقائق — لا يُحفظ)، واتخاذ قرار: **اعتماد**، **طلب تعديل** (يُطلب سبب يصل لمقدم الخدمة)، أو **رفض**. كل قرار يُسجَّل في سجل التدقيق.

## المستخدمون — `/admin/users`

بحث وفلترة حسب الدور (عميل/مقدم خدمة). تعليق أو إعادة تفعيل حساب. **لا يمكن للإدارة تعليق حسابها نفسها أو حساب إدارة آخر** — قيد مفروض في الخادم لمنع تعطيل النظام بالخطأ.

## التصنيفات — `/admin/categories`

إنشاء/تعديل/تعطيل التصنيفات الرئيسية (كهرباء، سباكة، ...). **لا يمكن تعطيل تصنيف يحمل مهنًا نشطة** — يجب تعطيل مهنه أولًا.

## المهن ومتطلبات المستندات — `/admin/professions`

الشاشة الأهم في اللوحة: لكل مهنة (نجّار، كهربائي، ...) تُحدَّد **متطلبات المستندات المطلوبة من مقدم الخدمة** ديناميكيًا — بلا أي تعديل كود أو نشر جديد:

- **تطلب مؤهلًا أو شهادة مهنية** و/أو **تطلب رخصة مزاولة مهنة**: تفعيل أي منهما يعيد بناء قائمة المستندات المقترحة تلقائيًا (بطاقة الرقم القومي + الصورة الشخصية + المؤهل/الرخصة حسب الاختيار).
- يمكن تخصيص القائمة يدويًا بعد ذلك: تفعيل/تعطيل كل مستند، تحديد الحد الأقصى لحجم الملف، أو إضافة **مستند مخصّص** بالكامل.
- التعديل يظهر فورًا في مسار التسجيل العام لمقدمي الخدمة الجدد — لا حاجة لإعادة نشر التطبيق.

⚠️ عند رفع سقف المتطلبات (مثلًا: تفعيل «تطلب رخصة» لمهنة كانت بلا رخصة سابقًا)، مقدمو الخدمة المُوثَّقون بالفعل **لا** يُطالَبون تلقائيًا بإعادة الرفع — الأثر على التسجيلات الجديدة فقط.

## الخدمات — `/admin/services`

إشراف على الخدمات المنشورة من مقدمي الخدمة: بحث بالعنوان، فلترة (ظاهرة/مخفية)، وإخفاء أي خدمة مخالفة دون حذفها نهائيًا.

## الطلبات — `/admin/orders`

قراءة فقط، لأغراض الدعم والتحقق — لا تعديل حالة الطلب من هنا (دورة حياة الطلب مملوكة للعميل ومقدم الخدمة). بحث برقم الطلب وفلترة بالحالة.

## التقييمات — `/admin/reviews`

إخفاء تقييم مخالف أو إظهاره مجددًا. **إخفاء/إظهار تقييم يعيد حساب متوسط تقييم مقدم الخدمة فورًا** من التقييمات الظاهرة فقط — لا يُحفظ رقم ثابت.

## الإشعارات العامة — `/admin/notifications`

بثّ إشعار داخل التطبيق لكل المستخدمين النشطين أو فئة منهم. سقف 5000 مستلم لكل بثّة، وحد 5 بثّات في الساعة لكل حساب إدارة — يمنع إساءة الاستخدام.

## الإعدادات — `/admin/settings`

قيم تشغيلية عامة (مثل الحد الأقصى لعدد صور الطلب، رقم الدعم). تعديلها يُطبَّق فورًا بلا إعادة نشر.

## سجل التدقيق — `/admin/audit-logs`

قراءة فقط — كل إجراء حسّاس (قرار توثيق، تعديل مهنة، تعليق حساب، بثّ إشعار...) مع مُنفِّذه ووقته. فلترة حسب نوع الكيان (مثال: `Profession`).

---

**ملاحظة أمنية عامة**: كل صفحات هذه اللوحة محمية على مستوى المسار (`src/proxy.ts`) وعلى مستوى كل نقطة API (`requireRole(request, 'ADMIN')`) بشكل مستقل — الحماية الفعلية دائمًا في الـAPI، لا في إخفاء الواجهة.
