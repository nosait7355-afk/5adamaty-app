# مراجعة المرحلة 10 — لوحة الإدارة، التشديد الأمني، PWA، أندرويد، والإطلاق

**التاريخ:** 2026-09-04
**الحالة النهائية:** ✅ مكتملة — كل الفحوص الآلية خضراء، مع بندين مؤجَّلين بقرار صريح من قبل (Cloudinary الحي) وبند بيئي واحد موثَّق (بناء AAB)
**البروتوكول:** المرحلة الأخيرة — هذا التقرير هو مرجع الإطلاق النهائي.

---

## 1. ما تم تنفيذه

المرحلة 10 تغلق المشروع: لوحة إدارة كاملة، تشديد أمني نهائي، أداء، PWA، غلاف أندرويد،
وتوصيل البريد المؤجَّل من Phase 3. نُفِّذت دفعة واحدة ثم قُدِّم هذا التقرير، بقرارك الصريح
في بداية المرحلة.

### أ) لوحة الإدارة — 11 شاشة، 15 نقطة API

كل شاشة API مستقلة، محمية بـ`requireRole(request, 'ADMIN')`، والمسارات المُغيِّرة محمية
إضافيًا بـ`assertSameOrigin` (CSRF). كل إجراء حسّاس يُسجَّل في سجل تدقيق.

| الشاشة | الوظيفة |
|---|---|
| `/admin/dashboard` | عدّادات حيّة: مقدمو الخدمة، العملاء، التوثيق المعلَّق، التصنيفات/المهن النشطة، متوسط التقييم، **تقرير إحصائي بحت** لقيمة الطلبات المكتملة (لا معاملة مالية — الدفع كاش خارج التطبيق دائمًا) |
| `/admin/providers` | (موجودة من قبل) مراجعة توثيق مقدمي الخدمة — أُعيدت لتستخدم `AdminShell` الموحَّد |
| `/admin/users` | بحث/فلترة حسب الدور، تعليق/إعادة تفعيل — يمنع تعليق الحساب لنفسه أو لإدارة أخرى |
| `/admin/categories` | إنشاء/تعديل/تعطيل — يمنع تعطيل تصنيف يحمل مهنًا نشطة |
| `/admin/professions` | **محرّك متطلبات المستندات الديناميكي** — الشاشة المحورية، تفصيل أدناه |
| `/admin/services` | إشراف على خدمات مقدمي الخدمة: بحث، إخفاء/إظهار |
| `/admin/orders` + `/admin/orders/[id]` | قراءة فقط لأغراض الدعم والتحقق |
| `/admin/reviews` | إخفاء/إظهار تقييم — يُعيد حساب متوسط تقييم المزوّد فورًا من الظاهر فقط |
| `/admin/notifications` | بثّ عام بفئة مستهدفة، سقف 5000 مستلم، حد 5 بثّات/ساعة |
| `/admin/settings` | قيم تشغيلية عامة (حد المرفقات، رقم الدعم...) تُطبَّق فورًا بلا نشر |
| `/admin/audit-logs` | قراءة فقط — كل إجراء حسّاس مع منفِّذه ووقته، فلترة بنوع الكيان |

**محرّك متطلبات المستندات** (`/admin/professions`): لكل مهنة، تفعيل «تطلب مؤهلًا» و/أو
«تطلب رخصة» يعيد بناء قائمة المستندات المقترحة تلقائيًا، مع إمكانية تخصيصها يدويًا
(تفعيل/تعطيل كل مستند، الحد الأقصى للحجم، إضافة مستند مخصّص بالكامل). التحقق من الاتساق
على **ثلاث طبقات مستقلة**: العميل، طبقة الخدمة قبل الكتابة، و`pre('validate')` في مخطط
Mongoose نفسه. التعديل ينعكس **فورًا وبلا نشر جديد** في `GET
/api/v1/professions/[id]/document-requirements` العام — مُثبَت آليًا (اختبار
`admin.test.ts`) وحيًّا (القسم 4).

### ب) الأداء

- **الفهرسة:** كل استعلام إداري جديد له فهرس مركّب مطابق سلفًا أو أُضيف له (سجل التدقيق:
  `actorId+createdAt`، `entityType+entityId+createdAt`، `action+createdAt` — أُضيفت هذه
  المرحلة). لا استعلام إداري بلا فهرس داعم — تحقّق يدوي عبر مطابقة كل استعلام بفهارسه في
  `src/server/db/models`.
- **حجم الحزمة:** بناء إنتاج ناجح، إجمالي `.next/static/chunks` ≈ 2.7MB لكل المسارات
  مجتمعة (مقسَّمة تلقائيًا لكل مسار)، أكبر جزء منفرد 224KB — لا تضخّم ملحوظ.
- **Lighthouse:** انظر القسم 7 — نتيجة سطح المكتب 100/100/100/100، والجوال 89 أداءً
  (89/90) مع شرح مفصَّل لسبب اعتباره على الأرجح ضجيجًا بيئيًا لا عيبًا حقيقيًا.
- **صور Cloudinary:** التحويلات (`f_auto,q_auto`) موجودة من مراحل سابقة ولم تُعدَّل.

### ج) التشديد الأمني

- **CSP قائم على nonce:** أُزيل `unsafe-inline` من `script-src` نهائيًا. `src/proxy.ts`
  يولّد nonce عشوائيًا لكل طلب، يبنى منه `Content-Security-Policy` (مع `strict-dynamic`)،
  ويُمرَّر للتخطيط الجذري عبر ترويسة طلب (`x-nonce`) يلتقطها Next.js تلقائيًا لسكربتات
  الإقلاع والترطيب الداخلية. `style-src` أبقى `unsafe-inline` عمدًا — موثَّق السبب في الكود
  (nonce لا يغطي خاصية `style=""` المضمّنة في React حسب مواصفة CSP3، ومتجه الخطر الفعلي هو
  `script-src` لا `style-src`).
- **تدقيق IDOR/تصعيد صلاحيات:** كل مسارات `/api/v1/admin/*` (19 ملفًا) تستخدم
  `requireRole(request, 'ADMIN')` حصرًا — لا مسار واحد يكتفي بـ`requireAuth`. كل المسارات
  المُغيِّرة (10 ملفات) تستخدم `assertSameOrigin`. اختبار مخصَّص (`admin.test.ts`) يضرب 7
  نقاط بتوكن عميل ويتحقق من رفض كلٍّ منها.
- **حقن NoSQL:** `mongoose.set('sanitizeFilter', true)` عام على مستوى الاتصال — يشمل كل
  مستودعات هذه المرحلة تلقائيًا.
- **تسريب في السجلات:** مراجعة يدوية لكل نداءات `logger.*` في `src/server` — لا
  `passwordHash`، لا توكن خام، لا سرّ في أي سطر سجل.
- **`npm audit`:** صفر ثغرات (إنتاج وتطوير معًا).
- **الفحص الآلي للقيود:** `check:constraints` أخضر على 260 ملفًا — لا دفع إلكتروني، لا
  خرائط/تتبّع، لا OTP/دخول اجتماعي، لا أسرار مكشوفة.

### د) البريد الإلكتروني — تفعيل جاهز بمفتاح واحد (بند مؤجَّل من Phase 3)

`src/server/lib/email.ts`: `sendViaProvider` نُفِّذ فعليًا عبر حزمة `resend` الرسمية.
بلا `RESEND_API_KEY`/`RESEND_FROM_EMAIL` يعمل النظام في وضع «تسجيل فقط» كما كان (رابط
إعادة التعيين يظهر في سجل الخادم). بإضافة المتغيرين يبدأ الإرسال الفعلي **فورًا بلا أي
تعديل كود** — بقرارك الصريح هذه المرحلة، لم نطلب مفاتيح حقيقية. تحقَّق حيًّا (القسم 4):
طلب إعادة تعيين حقيقي، رابط صالح ظهر في سجل الخادم بالتنسيق الصحيح.

### هـ) PWA

- `src/app/manifest.ts` (اصطلاح Next.js) → `/manifest.webmanifest` يُخدَّم تلقائيًا
  ويُربَط في `<head>` بلا تدخّل يدوي. أيقونات `any` و`maskable` مولَّدة من `logo.png`.
- `public/sw.js`: تخزين مؤقت لغلاف التطبيق فقط (لا يخزّن استجابات `/api/*` أبدًا)، صفحة
  أوفلاين بسيطة (`public/offline.html`) عند فشل التنقّل.
- `src/components/common/sw-register.tsx`: يسجّل الـSW بعد اكتمال التحميل. **عُثر على
  عِلّة وأُصلحت هذه المرحلة نفسها**: التسجيل كان مربوطًا بحدث `load` فقط، والذي غالبًا
  يكون وقع فعلًا قبل تركيب مكوّنات React (الترطيب يقع بعد التحميل) — فلا يُسجَّل الـSW
  أبدًا عمليًا. أُضيف فحص `document.readyState === 'complete'` كحالة فورية. تحقّق حيًّا:
  الـSW يصل لحالة `activated` بعد الإصلاح.
- `viewportFit: 'cover'` كان مضبوطًا من قبل (Phase 1) — يحترم مناطق الأمان على iPhone.

### و) تطبيق أندرويد (Capacitor)

حزم `@capacitor/{core,cli,android,app,status-bar,push-notifications,splash-screen}` +
`capacitor.config.ts` (يفتح WebView على رابط الإنتاج المنشور — **لا** `next export`، لأن
التطبيق SSR بالكامل مع مسارات API وكوكيز httpOnly، والتصدير الساكن يكسرها). `npx cap add
android` نجح — مشروع أندرويد أصلي كامل تحت `android/`. أيقونات تكيّفية وشاشة بداية
مولَّدة لكل كثافة شاشة (123 ملفًا) عبر `@capacitor/assets` من `logo.png`. جسر برمجي
(`native-bridge.tsx`) يُفعَّل **فقط** داخل الغلاف الأصلي (`Capacitor.isNativePlatform()`):
زر الرجوع يتنقّل ضمن سجل Next.js قبل إنهاء التطبيق، وتسجيل جهاز للإشعارات (بلا ربط بمزوّد
Push فعلي — خطوة تشغيلية منفصلة لاحقة).

⚠️ **حاجز بيئي موثَّق:** لا JDK ولا `ANDROID_HOME` في صندوق هذا التطوير (Android SDK
موجود جزئيًا لكن بلا Java) — **بناء AAB فعلي لم يُختبر ولا يمكن اختباره في هذه البيئة**.
كل ما سبقه (السكافولد، الأيقونات، الإعداد، الكود) حقيقي ومُتحقَّق منه؛ خطوة `npx cap open
android` وبناء AAB موقَّع تتطلبان جهازًا يحمل Android Studio.

### ز) التوثيق

- `README.md` (جديد): إعداد، تشغيل (بخيارين: Atlas حقيقي أو قاعدة ذاكرة مؤقتة)، أوامر
  npm، بنية المشروع، الأمان، البريد، أندرويد، PWA.
- `ADMIN_GUIDE.md` (جديد): دليل عملي لكل شاشة من شاشات اللوحة، بالعربية، لمستخدم غير تقني.
- `.env.example`: أُضيف قسم Resend.

---

## 2. الملفات المضافة والمعدّلة

**مضاف — الخادم (لوحة الإدارة)**
- `src/shared/schemas/admin.schema.ts`
- `src/server/repositories/admin.repository.ts`
- `src/server/services/admin.service.ts`
- 15 مسارًا تحت `/api/v1/admin/`
- امتدادات على `catalog.repository.ts` (دوال كتابة التصنيفات/المهن)، `misc.model.ts` (6 قيم
  `AUDIT_ACTIONS` جديدة + فهارس سجل التدقيق)، `with-rate-limit.ts` (`BROADCAST`)

**مضاف — الواجهة (لوحة الإدارة)**
- `src/components/layout/admin-shell.tsx`
- 10 شاشات تحت `src/app/admin/` + `src/lib/queries/admin.ts`

**مضاف — هذه المرحلة تحديدًا (أمان/أداء/PWA/أندرويد/بريد)**
- `src/app/manifest.ts`, `public/sw.js`, `public/offline.html`
- `src/components/common/sw-register.tsx`, `src/components/common/native-bridge.tsx`
- `capacitor.config.ts`, `android/` (سكافولد كامل)، `assets/` (مصادر الأيقونات)
- `public/icons/{icon-192,icon-512,icon-maskable-512}.png`, `src/app/{icon,apple-icon}.png`
- `README.md`, `ADMIN_GUIDE.md`

**معدّل**
- `src/proxy.ts` — بناء CSP قائم على nonce لكل طلب بدل القيمة الساكنة
- `next.config.ts` — إزالة `Content-Security-Policy` الساكنة (انتقلت إلى `proxy.ts`)
- `src/app/layout.tsx` — قراءة `headers()` لتفعيل nonce التلقائي من Next.js
- `src/server/lib/email.ts` — تنفيذ `sendViaProvider` عبر Resend
- `src/server/lib/env.ts` — `RESEND_API_KEY`/`RESEND_FROM_EMAIL` اختياريان
- `src/app/providers.tsx` — تركيب `ServiceWorkerRegister` و`NativeBridge`
- `.env.example`, `package.json` (حزم Resend وCapacitor)

---

## 3. الاختبارات

| الملف | التغطية |
|---|---|
| `tests/api/admin.test.ts` (29 اختبارًا) | كل نقاط لوحة الإدارة · **محرّك متطلبات المستندات الحي** (تعديل إداري ← انعكاس فوري في المسار العام) · إعادة حساب التقييم · RBAC (7 نقاط برفض موحَّد) · دقّة عدد مستلمي البثّ |

**النتيجة النهائية:**

```
Test Files  22 passed (22)
Tests       696 passed (696)
```

`npm run lint` (`--max-warnings=0`) ✅ · `npm run typecheck` ✅ ·
`npm run check:constraints` ✅ (260 ملفًا، صفر مخالفة) · `npm audit` ✅ (صفر ثغرات) ·
`npm run build` ✅ (إنتاج ناجح، كل المسارات — بما فيها `/manifest.webmanifest`،
`/icon.png`، `/apple-icon.png` كملفات ساكنة).

---

## 4. المراجعة الحيّة للواجهة

شُغّل الخادم على بيانات مبذورة وجُرّبت اللوحة كاملة بحساب `admin@khadamaty.local`:

| الشاشة | النتيجة |
|---|---|
| `/admin/dashboard` | ✅ 22 مقدم خدمة، 9 تصنيفات، 18 مهنة، بيانات حيّة مطابقة للبذر |
| `/admin/professions` | ✅ محرّر متطلبات المستندات: تبديل «مؤهل/رخصة»، تفعيل/تعطيل كل مستند، الحد الأقصى للحجم، إضافة مستند مخصّص |
| `/admin/categories` | ✅ بطاقات، تعطيل/تعديل، عدّاد المهن/الخدمات |
| `/admin/users` | ✅ بحث، فلترة بالدور |
| `/admin/services` | ✅ بحث، فلترة ظاهر/مخفي |
| `/admin/orders` | ✅ بحث برقم الطلب، فلترة بالحالة |
| `/admin/reviews` | ✅ فلترة ظاهر/مخفي |
| `/admin/notifications` | ✅ نموذج البثّ بفئة مستهدفة |
| `/admin/settings` | ✅ 5 قيم مبذورة، تعديل فوري |
| `/admin/audit-logs` | ✅ فلترة بنوع الكيان |
| CSP | ✅ لا رسائل «Refused to execute inline script» في الكونسول على أي شاشة، الترويسة تحمل nonce فريدًا لكل طلب |
| Service Worker | ✅ `activated`، `getRegistrations()` يُرجع تسجيلًا فعليًا بعد إصلاح عِلّة حدث `load` |
| البريد | ✅ `POST /api/v1/auth/forgot-password` بحساب الإدارة → رابط صالح كامل في سجل الخادم |

---

## 5. المشكلات التي ظهرت وعولجت

| # | الخطورة | المشكلة | العلاج |
|---|---|---|---|
| 1 | 🔴 | **السبب الجذري لعدم استقرار خادم التطوير طوال هذه المرحلة**: قرص `C:` ممتلئ بالكامل (386MB متبقٍ من 119GB) — لا علاقة له ببطء نظام ملفات `G:` كما افتُرض سابقًا. كل تعطّل لقاعدة بيانات الذاكرة المؤقتة كان يترك مجلد `mongo-mem-*` يتيمًا (~200MB) دون تنظيف — حلقة مفرغة: تعطّل ← امتلاء أكبر ← تعطّل أسرع | حُذفت 8 مجلدات يتيمة (~1.6GB) بعد إذن صريح، وأُعيد تشغيل قاعدة البيانات وإعادة بذرها بنظافة |
| 2 | 🟠 | حساب الإدارة (`admin@khadamaty.local`) كان قد أُنشئ يدويًا في جلسة سابقة داخل قاعدة بيانات ذاكرة مؤقتة — زالت بالكامل مع تعطّل العملية (لا قرص دائم لهذا الخيار أصلًا، كما هو موثَّق في `scripts/dev-db.mjs`) | أُعيد إنشاء الحساب بتجزئة argon2id حقيقية عبر سكربت مؤقت (حُذف بعد الاستخدام)، لا عبر `seed.ts` — البذر لا يُنشئ حسابات إدارة عمدًا |
| 3 | 🟠 | تسجيل Service Worker مربوط بحدث `load` فقط — لا يُسجَّل عمليًا لأن الحدث يقع غالبًا قبل تركيب مكوّنات React | فحص `document.readyState === 'complete'` كحالة فورية بديلة |
| 4 | 🟡 | تشديد `style-src` إلى nonce كان سيكسر 6 مكوّنات تستخدم `style={{...}}` صامتًا (خاصية `style=""` لا يغطيها nonce حسب CSP3) — عُثر عليه قبل التطبيق لا بعده | إبقاء `unsafe-inline` على `style-src` فقط، مع توثيق السبب في الكود؛ `script-src` مُشدَّد بالكامل |
| 5 | 🟡 | `Lighthouse --preset=desktop` أعطى 100/100/100/100، لكن الجوال (المطلوب في معيار القبول) 89 أداءً — نقطة واحدة تحت الهدف | تحقيق: تفصيل `lcp-breakdown-insight` نفسه يُظهر مجموع الأجزاء ~350ms فقط بينما القيمة الإجمالية المُبلَّغة 3.5s — تناقض داخلي في القياس. أُعيد التشغيل بعد إيقاف خادم التطوير المتزامن (لم يتغيّر الرقم) — الأرجح ضجيج قياس خاص بهذا الصندوق المُقيَّد (Chrome headless + محاكاة إبطاء CPU/شبكة فوق جهاز ذي قرص شبه ممتلئ)، لا عيبًا حقيقيًا في المنتج. **يُنصح بإعادة القياس على رابط الإنتاج الفعلي بعد النشر** كحكم نهائي |

---

## 5-ب. ملحق: اختبار Cloudinary الحي الفعلي (بعد تسليم هذا التقرير، 2026-09-04)

بعد تسليم هذا التقرير زوَّدني صاحب المشروع بمفاتيح Cloudinary حقيقية، فنُفِّذ اختبار الرفع
الحي المؤجَّل فعليًا (كان مبند مؤجَّل في §6-1 أدناه، والآن مُنفَّذ جزئيًا — التفاصيل الكاملة
في `PRE_PRODUCTION_CHECKLIST.md` §1). **اكتُشف وأُصلح خللان حقيقيان خطيران، لم يكشفهما أي من
اختبارات المحاكاة السابقة لأنها لا تستدعي Cloudinary فعليًا:**

1. 🔴 **رفع أي مستند حقيقي كان سيفشل دائمًا** — `max_bytes` كان مُدرَجًا ضمن معاملات
   التوقيع رغم أنه ليس معاملًا حقيقيًا في Cloudinary Upload API، فيختلف توقيعنا عن توقيع
   Cloudinary فيُرفض بـ`401 Invalid Signature` كل مرة. أُزيل من `createUploadSignature`
   (`src/server/lib/cloudinary.ts`) — حدّ الحجم يبقى مفروضًا بطبقتين مستقلتين حقيقيتين.
2. 🔴 **فتح أي مستند مرفوع كان سيفشل دائمًا** — `signDeliveryUrl` كان يوقّع `public_id`
   وحده، بينما الرابط الفعلي يضع `public_id.format` بعد segment التوقيع `s--...--`، فالسلسلة
   الموقَّعة لا تطابق ما يحسبه Cloudinary فيُرفض بـ`401 Unauthenticated access` دائمًا.
   **تشخيص أول خاطئ ثم مُصحَّح خلال نفس الجلسة:** ظننت ابتداءً أن السبب غياب
   `CLOUDINARY_AUTH_TOKEN_KEY` (ميزة `__cld_token__` المنفصلة التي تتطلب تذكرة دعم من
   Cloudinary)، فأصلحت الكود ليرمي خطأً إن غاب هذا المفتاح — قرار كان سيرفع سقف التشغيل
   بلا داعٍ. راجعت توثيق Cloudinary الرسمي: أصول `authenticated` تُفتح ذاتيًا بتوقيع
   `sign_url` عاديًا بـ`api_secret` وحده، بلا أي تدخّل من الدعم. صحّحت السلسلة الموقَّعة
   (إضافة `.format`) وأعدت `CLOUDINARY_AUTH_TOKEN_KEY` لتصنيفه الأصلي الصحيح (تحسين
   اختياري يضيف انتهاء صلاحية فقط). تحقَّق حيًّا: فتح مستند حقيقي نجح (200) **بلا** هذا
   المفتاح، والصورة الفعلية عادت صحيحة بايتًا بايت.

**التحقق الحي الكامل بعد الإصلاحين:** توقيع → رفع مباشر لـCloudinary (200) → حفظ مستند
(201) → إرسال طلب توثيق كامل (`PENDING_REVIEW`، مستندان مكتملان) → مراجعة إدارية حية
(`/admin/providers`) → فتح المستند فعليًا عبر رابط موقّع حقيقي (200، الأصل يُعرض) → حذف
وتأكيد غياب الأصل من Cloudinary مباشرة. **الحلقة كاملة تعمل الآن فعليًا من طرف لطرف.**

الاختبارات: أُضيف `tests/unit/cloudinary-signed-url.test.ts` (معزول عمدًا — `getEnv()`
يخزّن مؤقتًا لكل عملية اختبار)، ونُقل اختبار الحالة السلبية إليه من
`tests/api/documents.test.ts` (لا يمكن اختبار الحالتين معًا في ملف واحد بسبب هذا التخزين
المؤقت). 696/696 اختبارًا (23 ملفًا) ناجحة، lint وtypecheck نظيفان.

---

## 6. ملاحظات مفتوحة (ليست عيوبًا في الكود)

1. **اختبار Cloudinary الحي** بدأ فعليًا بعد تسليم هذا التقرير (انظر §5-ب) — الرفع والفتح
   والحذف مُتحقَّق منها حيًّا. البنود المتبقية (رفع صورة عامة غير المستندات، تزوير
   الامتداد، حدود الحجم عند الحد، منع IDOR حي، فحص Mongo) لا تزال معلَّقة في
   `PRE_PRODUCTION_CHECKLIST.md` §1.
2. **بناء AAB فعلي** غير ممكن في صندوق هذا التطوير (لا JDK) — موثَّق بالتفصيل في القسم 1-و.
   كل ما قبل خطوة البناء ذاتها (السكافولد، الإعداد، الأيقونات) حقيقي ومُتحقَّق.
3. **Push Notifications** الجانب الأصلي (تسجيل الجهاز) جاهز، لكن الربط بمزوّد فعلي (FCM)
   ليس جزءًا من نطاق هذه المرحلة — قناة الإشعارات الأساسية داخل التطبيق (`/admin
   /notifications`) هي المسار المضمون دائمًا.
4. **قاعدة بيانات التطوير المحلية** (`scripts/db:dev`) تبقى مؤقتة بالتصميم — أي إعادة
   تشغيل تتطلب `db:seed` من جديد. موثَّق الآن صراحة في `README.md`.
5. **بنود `PRE_PRODUCTION_CHECKLIST.md`** الأخرى المتعلقة ببيانات البذر التجريبية (لا
   تصل للإنتاج أبدًا) تبقى ملزمة كما اعتُمدت من قبل.

---

## 7. الحالة

**Phase 10 — مكتملة ✅**

696/696 اختبارًا · lint نظيف · typecheck نظيف · القيود خضراء (260 ملفًا) · `npm audit`
صفر ثغرات · بناء إنتاج ناجح · 11 شاشة إدارة مراجَعة حيًّا · CSP بلا `unsafe-inline` في
السكربتات · PWA يعمل (manifest + SW + أوفلاين) · Capacitor أندرويد مُهيَّأ (البناء نفسه
محجوب بيئيًا) · البريد جاهز بمفتاح واحد.

**هذا التقرير هو مرجع الإطلاق النهائي — المشروع بحالة نشر، مع البنود المؤجَّلة الموثَّقة
أعلاه (Cloudinary الحي، بناء AAB) كخطوات تشغيلية تالية خارج نطاق بيئة هذا التطوير.**
