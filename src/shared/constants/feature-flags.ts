/**
 * أعلام ميزات قيد الإعداد — تُفعَّل بتغيير القيمة هنا فقط، لا بحذف كود.
 */

/**
 * زر «الدخول/التسجيل عبر جوجل» في `/login` و`/register`.
 *
 * الكود والـbackend (`/api/v1/auth/google`) جاهزان بالكامل، لكن الزر مخفي
 * عمدًا لحين اكتمال إعداد Google Cloud Console (Client ID) واختباره.
 * فعّله بتغيير هذه القيمة إلى `true` — لا حاجة لأي تعديل آخر.
 */
export const GOOGLE_SIGN_IN_ENABLED = true;
