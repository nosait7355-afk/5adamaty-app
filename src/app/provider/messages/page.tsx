import { MessagesListScreen } from '@/components/features/messaging/messages-list-screen';

/** الرسائل — جانب مقدم الخدمة (تبويب «الرسائل» في الصور 25–29). */
export default function ProviderMessagesPage() {
  return <MessagesListScreen variant="provider" />;
}
