import { MessagesListScreen } from '@/components/features/messaging/messages-list-screen';

/** الرسائل — جانب العميل. */
export default function MessagesPage() {
  return <MessagesListScreen variant="customer" />;
}
