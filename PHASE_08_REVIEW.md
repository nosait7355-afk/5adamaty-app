# PHASE_08_REVIEW.md — دورة حياة الطلب (جانب مقدم الخدمة)

> المرجع: [`PROJECT_PLAN.md § Phase 8`](PROJECT_PLAN.md) · [`ARCHITECTURE.md §4`](ARCHITECTURE.md) · [`UI_ANALYSIS.md`](UI_ANALYSIS.md)
> الحالة: ✅ **مكتملة ومعتمدة** · 621/621 اختبار ناجح · بناء نظيف · الدورة كاملة مُختبَرة حيًّا حتى الإكمال
> آخر تحديث: تعديل رمز الإكمال بلا تأكيد إلى **422** بقرارك بعد الاعتماد (§13).

---

## 1. What was implemented

### الشاشات (24 · 25 · 26 · 27 · 28 · 29)

| المسار | الصورة | المحتوى |
|---|---|---|
| `/provider/dashboard` | 24 | بانر حالة الحساب · 4 KpiCards · بطاقة الأرباح الزرقاء بنسبة التغيّر · حلقة اكتمال الملف · أحدث 3 طلبات · شبكة أدوات المزوّد · بانر التوثيق |
| `/provider/orders` | 25 | 6 تبويبات بعدّادات · بحث برقم الطلب أو اسم العميل · ترتيب · بطاقات بزر أساسي **يتغيّر بالحالة** |
| `/provider/orders/[id]` | 26 → 29 | شاشة واحدة تتغيّر منطقة الإجراء فيها بالحالة: قبول/رفض (26) ← شبكة الحالات (27) ← صندوق التأكيد الأخضر (28) ← تأكيد استلام المبلغ (29) |

### الـAPI

`PATCH /orders/:id/status` · `POST /orders/:id/complete` · `GET /provider/orders` · `GET /provider/orders/:id` · `GET /provider/dashboard`

### حراسة الإكمال — ثلاث طبقات مستقلة

| الطبقة | الآلية | الرمز |
|---|---|---|
| قاعدة العمل | `assertCompletionConfirmed` — `false` أو الغياب | **422** `UNPROCESSABLE` برسالة عربية |
| الـState Machine | `requiresCashConfirmation` على انتقالَي الإكمال | 409 `INVALID_TRANSITION` |
| قاعدة البيانات | `pre('validate')` يرفض حفظ `COMPLETED` بلا تأكيد | — |

> المخطط يتحقق من **الشكل** فقط؛ نوع خاطئ (نص بدل قيمة منطقية) يبقى 400.

**ولا يمكن الالتفاف عليها:** `COMPLETED` **غير موجودة أصلًا** في مخطط `PATCH /status`، فمحاولة إرسالها ترتدّ بـ400 قبل أي منطق.

---

## 2. Files changed

### ملفات جديدة (10)

**الخادم**
`src/server/services/provider-orders.service.ts`
`src/app/api/v1/orders/[id]/status/route.ts` · `orders/[id]/complete/route.ts`
`src/app/api/v1/provider/orders/route.ts` · `provider/orders/[id]/route.ts` · `provider/dashboard/route.ts`

**الواجهة**
`src/app/provider/dashboard/page.tsx` · `src/app/provider/orders/page.tsx` · `src/app/provider/orders/[id]/page.tsx`
`src/components/features/orders/provider-order-card.tsx` · `src/components/common/status-stepper-h.tsx` · `src/lib/queries/provider-orders.ts`

**الاختبارات**
`tests/api/provider-orders.test.ts`

### ملفات معدّلة (8)

`src/shared/schemas/order.schema.ts` (تبويبات وبحث وترتيب المزوّد + مخططا التحديث والإكمال) · `src/shared/schemas/common.schema.ts` (`literalTrue`) · `src/shared/schemas/auth.schema.ts` و`provider.schema.ts` (استخدام المساعد) · `src/server/repositories/order.repository.ts` (بحث المزوّد + إحصاءات اللوحة + مصالحة العدّاد) · `src/server/services/order.service.ts` (تصدير الأدوات المشتركة) · `src/server/db/seed/seed.ts` (مسح الطلبات مع الإعدادات) · `src/lib/query-keys.ts` · `tests/api/orders.test.ts` و`tests/unit/order-state-machine.test.ts` (انحدارات جديدة)

---

## 3. Tests executed

```bash
npm run verify
```

## 4. Test results

| المجموعة | العدد | النتيجة |
|---|---|---|
| `tests/api/provider-orders.test.ts` | 39 | ✅ |
| `tests/unit/order-state-machine.test.ts` (+5 لحارس التأكيد) | 39 | ✅ |
| `tests/api/orders.test.ts` (+1 لانحدار العدّاد) | 41 | ✅ |
| المجموعات السابقة (Phase 1–7) | 502 | ✅ |
| **الإجمالي** | **621** | **✅ 621/621** (بعد تعديل 422) |

**تغطية ما طلبته الخطة نصًّا:**
- **وحدة: State Machine من جانب المزوّد + شرط الكاش** — الجدول الكامل (147 تركيبة) من Phase 7، مضافًا إليه أن تأكيد الكاش لا يفتح انتقالًا غير مسموح أصلًا.
- **API: قبول/رفض/تحديث/إكمال + منع مزوّد آخر + منع غير المعتمد** — كلها مغطّاة، مع التأكد أن الحالة **لا تتغيّر** بعد كل محاولة مرفوضة.
- **E2E: جديد ← قبول ← قيد التنفيذ ← في الطريق ← إكمال** — مع فحص `statusHistory` كاملًا وثبات `paymentMethod`.

## 5. Build result

✅ `next build` ينجح — 56 مسارًا (5 مسارات API و3 صفحات جديدة).
`npm run check:constraints`: **190 ملفًا، صفر مخالفة**.

---

## 6. UI review — مقارنة بالصور 24–29

فُحصت الرحلة كاملة حيًّا على `375×812`.

| العنصر | الصورة | الحالة |
|---|---|---|
| بانر «حسابك مفعّل» | 24 | ✅ |
| 4 KpiCards (جديدة/قيد التنفيذ/مكتملة هذا الشهر/التقييم) | 24 | ✅ حيًّا: 1 · 0 · 0 · 4.7 |
| بطاقة الأرباح الزرقاء + نسبة التغيّر | 24 | ✅ حيًّا: 120 ج.م بعد الإكمال، +100% |
| حلقة اكتمال الملف | 24 | ✅ |
| أحدث الطلبات + شبكة الأدوات + بانر التوثيق | 24 | ✅ |
| 6 تبويبات بعدّادات + بحث + ترتيب | 25 | ✅ |
| بطاقة: `#1002` + StatusBadge + اسم العميل وهاتفه والمنطقة والتاريخ + السعر + «طريقة الدفع: كاش» | 25 | ✅ |
| الزر الأساسي يتغيّر بالحالة (قبول ← تحديث ← لقد وصلت ← مكتمل) | 25 | ✅ مشتق من `availableActions` لا من اجتهاد الواجهة |
| StatusStepperH بأربع خطوات وطوابع زمنية | 27–29 | ✅ حيًّا: ✓✓✓ ثم الخطوة 4 نشطة |
| InfoAlert برتقالي «لا يوجد دفع أونلاين» | 27 | ✅ ظاهر في كل الحالات |
| بطاقة العميل + موقع الخدمة **نصي بحت** + اتصال/واتساب | 26 | ✅ لا خريطة ولا SDK |
| المرفقات مع EmptyState | 26 | ✅ «لا توجد مرفقات مع هذا الطلب» |
| أزرار قبول/رفض مع سبب إلزامي | 26 | ✅ |
| شبكة اختيار الحالة + «حفظ تحديث حالة الطلب — سيتم إشعار العميل» | 27 | ✅ |
| صندوق أخضر «تأكيد إكمال الخدمة» بنقطتَي checklist | 28 | ✅ |
| checkbox تأكيد استلام المبلغ + ملاحظة «لا خدمة دفع إلكترونية» + زر معطّل حتى التأشير | 29 | ✅ مؤكَّد حيًّا: معطّل → معطّل بخانة واحدة → مفعّل بالخانتين |

**انحراف مقصود:** الصور 26–29 أربع شاشات منفصلة، ونفّذتها **شاشة واحدة تتغيّر منطقة الإجراء فيها بالحالة**. السبب: محتواها العلوي متطابق حرفيًا (نفس البطاقات والترتيب)، وفصلها كان سيكرّر نفس الشيفرة أربع مرات ويخاطر باختلافها. التسلسل البصري الذي ترسمه الصور محفوظ كما هو.

---

## 7. Security review

| البند | الحالة |
|---|---|
| المزوّد يرى/يعدّل طلباته فقط | ✅ مزوّد آخر يحصل على **404** لا 403، في القائمة والتفاصيل والتحديث والإكمال — و**الحالة لا تتغيّر** بعد المحاولة (مُختبَر صراحةً). |
| التحقق من `verification.status === APPROVED` قبل أي إجراء | ✅ `assertCanAcceptOrders` تُستدعى عند **كل** إجراء لا عند الإنشاء فقط — الموافقة قد تُسحب بعد منحها. |
| منع الإكمال بدون تأكيد الاستلام على مستوى الـService | ✅ ثلاث طبقات مستقلة: `assertCompletionConfirmed` (**422**) + الـState Machine (409) + نموذج قاعدة البيانات. ولا مسار بديل: `COMPLETED` غير موجودة في مخطط `/status`. |
| لا كيان `Transaction` أو `Payment` | ✅ فحص القيود على 190 ملفًا، واختبار يفحص أن استجابة اللوحة لا تحوي: wallet · balance · transaction · payout · invoice · visa · stripe · paymob. |
| «في الطريق» بلا موقع | ✅ تغيير حالة يدوي بحت — لا استدعاء موقع ولا GPS ولا ETA ولا مسافة في أي طبقة. |
| بيانات العميل للمزوّد | ✅ تظهر منذ `NEW` — والاتجاه المعاكس مقيّد (العميل يرى هاتف المزوّد بعد القبول فقط). **الاتجاهان غير متماثلين عمدًا**: العميل هو من بدأ التواصل بإرسال الطلب، والمزوّد يحتاج البيانات ليقرّر القبول أصلًا (الصورة 26 تعرضها على طلب جديد). |
| حقن تعبير نمطي في بحث المزوّد | ✅ النص مهرَّب — `.*` يعيد صفر نتيجة لا كل الطلبات (مُختبَر). |
| سباق التزامن | ✅ التحديث الشرطي الذرّي نفسه من Phase 7. |
| سبب الرفض إلزامي | ✅ 400 بلا سبب — يصل نصّه للعميل في الإشعار. |

---

## 8. Issues found

### 🔴 (1) شاشة تفاصيل الطلب للمزوّد كانت تتعطّل بالكامل

`useProviderOrder` كانت تنادي `GET /orders/:id` — مسار **العميل**، الذي يعيد `provider` بلا `customer`. فتنهار الشاشة عند قراءة `data.customer.phone` بـ`Cannot read properties of undefined`. **اكتُشف بالتجريب الحيّ**: كل الاختبارات كانت تمرّ لأنها تستدعي الخدمة عبر مسارات أخرى، ولم يكن هناك مسار `GET /provider/orders/:id` أصلًا.

### 🟠 (2) عدّاد أرقام الطلبات يُصدر أرقامًا مستعملة

`db:seed --clear` يمسح `settings` — حيث يعيش عدّاد أرقام الطلبات — بينما تبقى الطلبات في مجموعتها. فيعود العدّاد إلى 1 ويصطدم بالفهرس الفريد: `E11000 duplicate key error … orderNumber: 1001`. **اكتُشف بالتجريب الحيّ** عند أول محاولة إنشاء طلب بعد إعادة البذر. الخطر ليس تطويريًا فقط: أي استعادة نسخة احتياطية جزئية على الإنتاج تُنتج نفس العطل.

### 🟡 (3) رسالة إنجليزية تصل للمستخدم

إرسال `cashReceivedConfirmed: false` صراحةً كان يُنتج **«Invalid literal value, expected true»** بدل الرسالة العربية. السبب: `z.literal(true, { message })` يطبّق الرسالة على خطأ **النوع** فقط، أما القيمة الخاطئة فتقع في مسار آخر. نفس العلّة كانت في `acceptTerms` بمخططَي تسجيل العميل والمزوّد.

---

## 9. Issues fixed — كلها (3/3)

| # | الإصلاح | التحقق |
|---|---|---|
| 1 | مسار جديد `GET /provider/orders/:id` يعيد شكل المزوّد (بيانات العميل + `availableActions`)، وتوجيه الاستعلام إليه | 4 اختبارات جديدة (البيانات · مزوّد آخر 404 · العميل 403 · `canComplete`) + المرور الحيّ الكامل |
| 2 | **حزامان**: العدّاد يصالح نفسه عند أول زيادة (يقرأ أعلى رقم موجود ويقفز فوقه)، والبذر صار يمسح الطلبات مع الإعدادات فلا تتفكك القاعدة أصلًا | اختبار انحدار يمسح العدّاد ويتأكد أن الرقم التالي أكبر · حيًّا: تخطّى 1001 المستعمل وأصدر **1002** |
| 3 | مساعد مشترك `literalTrue(message)` بـ`errorMap` يغطّي كل المسارات، مطبَّق على الحقول الأربعة | 4 اختبارات تفحص **غياب أي حرف لاتيني** في الرسالة |

---

## 10. Remaining issues

| # | البند | الأثر | الجهة |
|---|---|---|---|
| 1 | `CRUD /provider/services` غير مبنيّ | **قرار معتمد منك بعد المراجعة: لا يُنفَّذ حاليًا** — لا شاشة له في التصميمات المعتمدة، ولا نخترع شاشة أو ميزة خارج مواصفة الواجهة. يُعاد النظر فيه مستقبلًا عند الحاجة | مؤجَّل بقرار |
| 2 | «مناطق التغطية» تُدار من `/provider/profile` لا من شاشة مستقلة | نفس السبب: لا شاشة لها في الصور، والحقل موجود في معالج التسجيل | Phase 9/10 |
| 3 | «الرسائل» في BottomNav للمزوّد غير مفعّلة | نظام المراسلة من نطاق Phase 9 | Phase 9 |
| 4 | «التقييمات» و«العملاء» في شبكة الأدوات تقودان لمسارات غير مبنية | Phase 9 | Phase 9 |
| 5 | زر «مشاركة الطلب» ينسخ الرابط فقط | مقبول | — |

**القراران بعد المراجعة (معتمدان):**
1. `CRUD /provider/services` — **لا يُنفَّذ حاليًا**، التزامًا بقاعدة عدم اختراع شاشات خارج التصميم المعتمد.
2. رمز الإكمال بلا تأكيد — **عُدِّل إلى 422** كما هو منصوص في معيار القبول. التفاصيل في §11 و§13.

---

## 11. Acceptance Criteria — التحقق

| # | المعيار | النتيجة |
|---|---|---|
| 1 | `POST /complete` بدون `cashReceivedConfirmed` يعيد **422** | ✅ **مُعدَّل بقرارك بعد اعتماد المرحلة.** المخطط صار يتحقق من الشكل فقط، وانتقل اشتراط التأكيد إلى `assertCompletionConfirmed` في الخدمة فترتدّ المحاولة بـ**422 UNPROCESSABLE** برسالة عربية. الشكل الخاطئ (نص بدل قيمة منطقية) يبقى 400 — وهو التمييز الصحيح بين خطأ تحقّق ومخالفة قاعدة عمل. |
| 2 | لا يوجد أي كيان `Transaction` أو `Payment` في الكود | ✅ فحص القيود (190 ملفًا) + اختبار يفحص 8 مصطلحات مالية في استجابة اللوحة |
| 3 | «في الطريق» بلا أي استدعاء موقع/GPS | ✅ تغيير حالة يدوي بحت في كل الطبقات |

**Definition of Done:** كل الميزات مكتملة عدا البند المذكور في §10 ✅ · 618/618 اختبار ✅ · بناء نظيف ✅ · مقارنة بصرية بالصور 24–29 ✅ · مراجعة أمنية ✅ · دورة كاملة مُختبَرة حيًّا (طلب → قبول → تنفيذ → إكمال بتأكيد الاستلام → تحديث الأرباح) ✅ · تقرير المراجعة ✅

---

## 12. تشغيل المراجعة محليًا

```bash
node scripts/dev-db.mjs
```

ثم في نافذة أخرى:

```bash
npm run db:seed -- --clear && npm run dev
```

> `--clear` صار يمسح الطلبات والتقييمات والإشعارات مع الإعدادات، فلا تتفكك القاعدة.

حسابات المراجعة (**تطويرية فقط** — `PRE_PRODUCTION_CHECKLIST §2`):
- **عميل:** `01033334444` / `Customer12345`
- **مقدم خدمة:** `plumber@dev.local` / `Provider12345` (الطلب `#1002` مكتمل)
- **إدارة:** تُنشأ بالسكربت المذكور في تقرير Phase 6

---

## 13. تعديل ما بعد الاعتماد — رمز 422

**الطلب:** الالتزام بمعيار القبول حرفيًا — محاولة الإكمال بلا تأكيد استلام المبلغ تعيد **422** لا 400، برسالة عربية، مع بقاء المنع في كل الطبقات.

**ما تغيّر:**

| قبل | بعد |
|---|---|
| `completeOrderSchema` يشترط `literal(true)` → 400 من طبقة التحقق | المخطط يتحقق من **الشكل** فقط (`z.boolean().optional()`) |
| قاعدة «يجب أن تكون true» داخل المخطط | قاعدة عمل مستقلة في `assertCompletionConfirmed` → **422 `UNPROCESSABLE`** |
| — | الحارس يُستدعى **كأول سطر** في `completeOrder`، قبل أي قراءة أو تحقق آخر |

**السلوك بعد التعديل:**

| المُرسَل | الرمز | الرسالة |
|---|---|---|
| `{}` | **422** | «لا يمكن إكمال الطلب قبل تأكيد إتمام الخدمة بالكامل.» |
| `{ serviceCompleted: true }` | **422** | «لا يمكن إكمال الطلب قبل تأكيد استلام المبلغ من العميل.» |
| `{ cashReceivedConfirmed: true }` | **422** | «لا يمكن إكمال الطلب قبل تأكيد إتمام الخدمة بالكامل.» |
| `{ serviceCompleted: true, cashReceivedConfirmed: false }` | **422** | «… تأكيد استلام المبلغ …» |
| `{ serviceCompleted: 'yes' }` (نوع خاطئ) | 400 | خطأ تحقّق — شكل لا قاعدة |
| التأكيد الكامل | 200 | يكتمل الطلب |

**لماذا بقي 400 لخطأ النوع:** التمييز مقصود ومفيد للعميل — 400 يعني «أصلح شكل الطلب»، و422 يعني «الطلب سليم الشكل لكن ينقصه شرط عمل». دمجهما كان سيُضيّع المعلومة.

**الطبقات الثلاث ما زالت قائمة:** لا تسمح أي منها بالإكمال بلا تأكيد — الحارس (422)، والـState Machine (409)، ونموذج قاعدة البيانات. أُضيف اختبار يجرّب **خمس تركيبات ناقصة** ويتأكد بعدها أن حالة الطلب لم تتغيّر وأن `cashReceivedConfirmed` ما زال `false`.

**التحقق:** `npm run verify` أخضر بالكامل — **621/621 اختبار** · 190 ملفًا بصفر مخالفة · بناء نظيف.
