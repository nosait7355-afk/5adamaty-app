# PHASE_07_REVIEW.md — دورة حياة الطلب (جانب العميل)

> المرجع: [`PROJECT_PLAN.md § Phase 7`](PROJECT_PLAN.md) · [`ARCHITECTURE.md §4`](ARCHITECTURE.md) · [`UI_ANALYSIS.md`](UI_ANALYSIS.md)
> الحالة: ✅ **مكتملة** · 576/576 اختبار ناجح · بناء نظيف · الرحلة كاملة مُختبَرة حيًّا على المتصفح

---

## 0. قبل المرحلة — تثبيت قائمة ما قبل الإنتاج

بناءً على قرارك، أُنشئ ملف مُلزِم جديد: [`PRE_PRODUCTION_CHECKLIST.md`](PRE_PRODUCTION_CHECKLIST.md)

**§1 — اختبار Cloudinary الحقيقي (البنود التسعة كما طلبتها حرفيًا):** رفع صورة حقيقية · رفع مستندات حقيقية · التحقق من MIME/النوع · حدود الحجم · Signed URLs · انتهاء صلاحيتها · منع الوصول لمستند مستخدم آخر · الحذف/الاستبدال · التأكد أن MongoDB لا يحوي ملفات أو بيانات ثنائية (مع أوامر الفحص الفعلية على قاعدة الإنتاج).

**§2 — بيانات التطوير والمراجعة ممنوعة على الإنتاج:** جدول يسمّي كل حساب استُخدم في المراجعة (Phase 5 و6) وبيانات البذر، وما يُسمح ببذره (بيانات مرجعية لا حسابات).

**الحواجز لم تبقَ ورقية — أُضيفت 3 اختبارات دائمة** في `tests/db/seed.test.ts`:
1. كل حسابات البذر تحمل نطاق `@seed.local` — لا نطاق حقيقي.
2. لا حساب بذر يملك تجزئة `argon2id` صالحة — **الدخول بأي منها مستحيل**.
3. لا حساب `ADMIN` في البذر إطلاقًا.

كما رُبطت القائمة من `PROJECT_PLAN.md` كقاعدة عابرة للمراحل.

---

## 1. What was implemented

### الـState Machine — قلب المرحلة

`src/server/policies/order-state-machine.ts`: جدول انتقالات صريح `(from, to, roles[])` هو **المصدر الوحيد** لما يجوز. دوال خالصة بلا قاعدة بيانات ولا HTTP، تستدعيها كل الطبقات:

| من | إلى | الدور |
|---|---|---|
| NEW | ACCEPTED · REJECTED | PROVIDER |
| NEW · ACCEPTED | CANCELLED | CUSTOMER · ADMIN |
| ACCEPTED | IN_PROGRESS · ON_THE_WAY | PROVIDER |
| IN_PROGRESS ⇄ ON_THE_WAY | — | PROVIDER |
| IN_PROGRESS · ON_THE_WAY | COMPLETED | PROVIDER **+ تأكيد استلام المبلغ** |

12 انتقالًا مسموحًا من أصل 147 تركيبة ممكنة. كل ما عداها مرفوض بـ**409 `INVALID_TRANSITION`** أو **403** إن كان الرفض بسبب الدور.

### الشاشات (11 · 12 · 13 · 14)

| المسار | الصورة | المحتوى |
|---|---|---|
| `/orders/new` | 11 → 12 | معالج 4 خطوات: تفاصيل ← تأكيد ← اختيار الوقت ← تم الإرسال، ببطاقة المزوّد والسعر المبدئي وتنبيه الدفع الأصفر |
| `/orders` | 13 | تبويبات بعدّادات (الكل/قيد التنفيذ/مكتملة/ملغاة) + بطاقات بأزرار سياقية + بطاقة الدعم |
| `/orders/[id]` | 14 | الحالة والتاريخ · بطاقة المزوّد + اتصال/واتساب · جدول 7 صفوف · Timeline من `statusHistory` · طلب مرة أخرى · مشاركة · إلغاء (أحمر) · تنبيه الدفع |

### الـAPI

`POST /orders` · `GET /orders` · `GET /orders/:id` · `POST /orders/:id/cancel`

---

## 2. Files changed

### ملفات جديدة (11)

**الخادم**
`src/server/policies/order-state-machine.ts` · `src/shared/schemas/order.schema.ts` · `src/server/repositories/order.repository.ts` · `src/server/services/order.service.ts`
`src/app/api/v1/orders/route.ts` · `orders/[id]/route.ts` · `orders/[id]/cancel/route.ts`

**الواجهة**
`src/app/orders/page.tsx` · `src/app/orders/[id]/page.tsx` · `src/app/orders/new/page.tsx`
`src/components/features/orders/order-card.tsx` · `src/lib/queries/orders.ts`

**التوثيق والاختبارات**
`PRE_PRODUCTION_CHECKLIST.md` · `tests/unit/order-state-machine.test.ts` · `tests/api/orders.test.ts`

### ملفات معدّلة (3)

`src/lib/query-keys.ts` · `PROJECT_PLAN.md` (ربط قائمة ما قبل الإنتاج) · `tests/db/seed.test.ts` (حواجز بيانات البذر)

---

## 3. Tests executed

```bash
npm run verify
```

## 4. Test results

| المجموعة | العدد | النتيجة |
|---|---|---|
| `tests/unit/order-state-machine.test.ts` | 34 | ✅ |
| `tests/api/orders.test.ts` | 40 | ✅ |
| `tests/db/seed.test.ts` (+3 لحواجز البذر) | 17 | ✅ |
| المجموعات السابقة (Phase 1–6) | 485 | ✅ |
| **الإجمالي** | **576** | **✅ 576/576** |

**تغطية ما طلبته الخطة نصًّا:**
- **جدول الانتقالات كاملًا (المسموح والممنوع):** اختبار واحد يمرّ على **147 تركيبة** (7×7×3) ويؤكد أن المسموح هو 12 بالضبط، لا أكثر. أي حالة تُضاف مستقبلًا تدخل الفحص تلقائيًا.
- **API:** الإنشاء، القوائم بالحالة والتبويب، الإلغاء المسموح والممنوع، IDOR.
- **E2E:** إنشاء ← ظهور في «طلباتي» ← تفاصيل ← إلغاء ← انتقاله لتبويب «ملغاة».

## 5. Build result

✅ `next build` ينجح — 50 مسارًا (3 مسارات API و3 صفحات جديدة).
`npm run check:constraints`: **178 ملفًا، صفر مخالفة**.

---

## 6. UI review — مقارنة بالصور 11–14

فُحصت الرحلة كاملة حيًّا على `375×812` ببيانات حقيقية.

| العنصر | الصورة | الحالة |
|---|---|---|
| Stepper أربع خطوات يُقرأ من اليمين، المنجز ✓ | 11 · 12 | ✅ |
| بطاقة المزوّد + السعر المبدئي ثابتة أعلى كل خطوة | 11 · 12 | ✅ `250 - 1,200 ج.م` حيًّا |
| نوع الخدمة · تفاصيل 0/500 · المنطقة + العنوان · التاريخ · ملاحظات | 11 | ✅ |
| تنبيه «الدفع عند تنفيذ الخدمة» الأصفر | 11 | ✅ |
| ✓ + «تم إرسال طلبك بنجاح!» + رقم الطلب | 12 | ✅ `#1001` حيًّا |
| جدول ملخص 7 صفوف | 12 | ✅ |
| تنبيه أصفر «الدفع بعد تنفيذ الخدمة مباشرة» | 12 | ✅ |
| «ماذا يحدث الآن؟» (3 خطوات) | 12 | ✅ |
| تبويبات بعدّادات | 13 | ✅ `الكل (1)` · `قيد التنفيذ (1)` · `مكتملة (0)` حيًّا |
| بطاقة: `#1001` + StatusBadge + 📍📅🕐 + بطاقة سعر ملوّنة بالحالة + شريط المزوّد | 13 | ✅ |
| أزرار سياقية بحسب الحالة | 13 | ✅ تحكمها أعلام من الخادم لا اجتهاد الواجهة |
| StatusBadge + تاريخ الإنشاء + بطاقة المزوّد + «عرض الملف» | 14 | ✅ |
| جدول تفاصيل 7 صفوف + Timeline + إلغاء أحمر + تنبيه الدفع | 14 | ✅ |
| الأرقام لاتينية داخل النص العربي | §1.2 | ✅ |

**انحراف مقصود، مع سببه:** الـStepper يسمّي الخطوة 3 «اختيار الوقت» بينما الصورة 11 تعرض التاريخ **والوقت** في الخطوة 1. نفّذت ما هو مرسوم حرفيًا: التاريخ (إلزامي) في الخطوة 1، والخطوة 3 مخصّصة لضبط **نافذة الوقت المفضّلة** بأربع نوافذ جاهزة أو تخطّيها بـ«أي وقت». هذا يحترم اسم الخطوة في الـStepper ولا يكرّر حقلًا بلا معنى.

---

## 7. Security review

| البند | الحالة |
|---|---|
| **لا وجود لأي حقل أو مسار دفع** | ✅ `paymentMethod` **لا يُقرأ من المدخلات إطلاقًا** — الخادم يثبّته على `CASH_ON_DELIVERY_OFFLINE`. اختبار يفحص أن استجابة الطلب لا تحوي أيًّا من: visa · mastercard · stripe · paypal · paymob · fawry · wallet · checkout · transaction. |
| `agreedPrice` غير قابل للتعديل من العميل | ✅ غير موجود في مخطط الإدخال أصلًا؛ يُشتق من سعر الخدمة أو `priceMin` للمزوّد. محاولة تمريره ترتد بـ400 (`.strict()`). |
| `status` غير قابل للتعديل من العميل | ✅ نفس الشيء — و5 محاولات مختلفة (`status`, `paymentMethod`, `agreedPrice`, `cashReceivedConfirmed`, `orderNumber`) ترتد كلها بـ400. |
| IDOR | ✅ العميل الآخر ← **404** لا 403، ومقدم خدمة آخر ← 404، ومحاولة الإلغاء من غير المالك تُرفض **ولا تغيّر الحالة** (مُختبَر بالتأكد من بقاء `NEW`). |
| مزوّد غير معتمد لا يستقبل طلبات | ✅ نفس حارس Phase 6 (`assertCanAcceptOrders`) يُستدعى قبل الإنشاء — 403. |
| لا طلب من النفس | ✅ 422. |
| تسريب هاتف المزوّد | ✅ **لا يُعرض قبل قبول الطلب**. قبل القبول: `isContactUnlocked: false` ولا مفتاح `phone` في الاستجابة أصلًا — مُختبَر نصيًّا. |
| سباق التزامن | ✅ التحديث شرطي وذرّي (`{_id, status: from}`): لو ألغى العميل ونفس اللحظة قبِل المزوّد، ينجح أولهما فقط ويعود الثاني بـ409 بدل الدهس. |
| كل انتقال موثّق | ✅ `statusHistory` تُكتب في **نفس** العملية الذرّية للتحديث — يستحيل أن تتغيّر الحالة بلا سطر يوثّقها. مع `auditLog` وإشعار الطرف الآخر. |
| روابط الاتصال | ✅ `tel:` و`wa.me` روابط نظام لا تكاملات: لا مفتاح API ولا خدمة وسيطة ولا تتبّع. |

---

## 8. Issues found

### 🟠 (1) المعاملات كانت تُسقط كل عمليات الإنشاء

ARCHITECTURE §4 تشترط كتابة الانتقال والتدقيق والإشعار في **معاملة واحدة**. تنفيذي الأول جرّب فتح معاملة ثم قرأ نص الخطأ عند الفشل — لكن `mongod` المنفرد (بيئة الاختبار) لا يفشل عند `startTransaction` بل عند أول كتابة، برسالة «does not support retryable writes» لم يكن الالتقاط يطابقها. **31 اختبارًا من 40 سقط.**

### 🟡 (2) رمز الخطأ كان `CONFLICT` بدل `INVALID_TRANSITION`

ARCHITECTURE §4 تنصّ حرفيًا على `409 INVALID_TRANSITION`. استخدامي `conflict()` العام كان يُنتج `CONFLICT`، فيفقد العميل القدرة على التمييز بين «تعارض» و«انتقال مرفوض» بلا قراءة نص الرسالة.

### 🟡 (3) رسالة مضلّلة عند إعادة إلغاء طلب ملغى

اكتُشف **بالتجريب الحيّ**: إعادة الإلغاء كانت تقول «لا يمكن نقل الطلب من ملغي إلى ملغي» لأن فحص `from === to` يسبق فحص الحالة النهائية. الرسالة الصحيحة هي «الطلب في حالة ملغي ولا يمكن تغييرها».

### 🟡 (4) حساب المراجعة من Phase 5 لم يعد موجودًا

`npm run db:seed -- --clear` في Phase 6 يحذف مستخدمي `CUSTOMER`/`PROVIDER`، فاختفى حساب `01011112222`. ليس خللًا في المنتج لكنه يُبطل ما ذُكر في تقرير Phase 6.

---

## 9. Issues fixed — كلها (4/4)

| # | الإصلاح | التحقق |
|---|---|---|
| 1 | استبدال تخمين نص الخطأ بسؤال الخادم مرة واحدة عن دعم المعاملات (`hello` → `setName`/`isdbgrid`) مع تخزين الجواب. الإنتاج على Atlas يعمل بمعاملة حقيقية، والاختبار المنفرد ينفّذ نفس المنطق بلا معاملة — والأمان لا يعتمد عليها أصلًا بل على التحديث الشرطي الذرّي | 40/40 اختبار API |
| 2 | `invalidTransition()` في كل رفض انتقال (والرمز كان معرّفًا وغير مستخدم منذ Phase 1) | اختبار يفحص `error.code === 'INVALID_TRANSITION'` + تأكيد حيّ |
| 3 | تقديم فحص الحالة النهائية على فحص التطابق | اختبار جديد يفحص الرسالة نصًّا + تأكيد حيّ |
| 4 | حسابات مراجعة جديدة موثّقة في §12 أدناه | تسجيل ودخول فعليان |

---

## 10. Remaining issues

| # | البند | الأثر | الجهة |
|---|---|---|---|
| 1 | رفع صور المرفقات لم يُختبر حيًّا (لا بيانات Cloudinary) | المخطط والتحقق من الحد الأقصى (5) مُختبَران، والرفع نفسه مؤجَّل بقرارك | `PRE_PRODUCTION_CHECKLIST §1` |
| 2 | المعاملات لم تُختبر على مجموعة نسخ حقيقية | المسار المنفرد مُختبَر بالكامل؛ مسار المعاملة يحتاج Atlas | قبل الإطلاق |
| 3 | «تقييم الخدمة» يقود إلى `/orders/[id]/review` غير المبنية | شاشة التقييم من نطاق Phase 9 | Phase 9 |
| 4 | 💬 «محادثة» في الصورة 13 غير مفعّلة | نظام المراسلة من نطاق Phase 9 | Phase 9 |
| 5 | تغيير الحالة من جانب المزوّد (قبول/رفض/تنفيذ/إكمال) غير مبنيّ | نطاق Phase 8 — الـState Machine جاهزة له بالكامل، والاختبارات تستخدم المستودع مباشرة مؤقتًا | Phase 8 |
| 6 | زر «مشاركة الطلب» يعتمد `navigator.share` مع سقوط إلى النسخ | لا يعمل كمشاركة أصلية على متصفحات سطح المكتب | مقبول |

**ملاحظة بيئة (تكرّرت من Phase 6):** تشغيل الاختبارات وخادم التطوير معًا يجعل إقلاع `mongod` يتجاوز مهلته أحيانًا فيسقط ملف اختبار قاعدة بيانات. بعد إيقاف خادم المعاينة: **19/19 ملف و576/576 اختبار**.

---

## 11. Acceptance Criteria — التحقق

| # | المعيار | النتيجة |
|---|---|---|
| 1 | أي انتقال غير مسموح يعيد **409** | ✅ بالرمز المنصوص عليه `INVALID_TRANSITION` — مؤكَّد حيًّا وباختبار |
| 2 | لا يمكن إلغاء طلب بعد `IN_PROGRESS` | ✅ مُختبَر على `IN_PROGRESS` و`ON_THE_WAY` — 409 |
| 3 | Timeline يطابق `statusHistory` **حرفيًا** | ✅ اختبار يقارن المصفوفتين عنصرًا بعنصر بعد انتقالين |
| 4 | التنبيهات الصفراء للدفع الكاش ظاهرة في 11 و12 و14 | ✅ مؤكَّدة بصريًا في الثلاث |
| 5 | `paymentMethod` قيمة ثابتة واحدة | ✅ مُثبت على كل طلبات القاعدة، ولا يُقرأ من المدخلات |

**Definition of Done:** كل الميزات مكتملة ✅ · 576/576 اختبار ✅ · بناء نظيف ✅ · مقارنة بصرية بالصور 11–14 ✅ · مراجعة أمنية ✅ · رحلة كاملة مُختبَرة حيًّا (طلب → إرسال → طلباتي → تفاصيل → إلغاء → 409 عند التكرار) ✅ · تقرير المراجعة ✅

---

## 12. تشغيل المراجعة محليًا

```bash
node scripts/dev-db.mjs
```

ثم في نافذة أخرى:

```bash
npm run db:seed -- --clear && npm run dev
```

> ⚠️ `--clear` يحذف حسابات `CUSTOMER`/`PROVIDER` — أعد إنشاء حسابات المراجعة بعده.

حسابات المراجعة الحالية (**تطويرية فقط** — انظر `PRE_PRODUCTION_CHECKLIST §2`):
- **عميل:** `01033334444` / `Customer12345` (الطلب `#1001`)
- **إدارة:** `admin@khadamaty.local` / `AdminReview123`
- **مقدم خدمة معتمد:** `mostafa@example.com` / `Provider12345`
