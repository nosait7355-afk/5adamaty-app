# PHASE_05_REVIEW.md — الكتالوج والاكتشاف

> المرجع: [`PROJECT_PLAN.md § Phase 5`](PROJECT_PLAN.md) · [`UI_ANALYSIS.md`](UI_ANALYSIS.md) · [`ARCHITECTURE.md`](ARCHITECTURE.md)
> الحالة: ✅ **مكتملة** · 422/422 اختبار ناجح · بناء نظيف · تحقّق حيّ على المتصفح

---

## 1. What was implemented

### الشاشات (06 · 07 · 08 · 09 · 10 + البحث)

| المسار | الصورة | المحتوى |
|---|---|---|
| `/home` | 06 | بحث · بانر بـcarousel · صف 6 مربعات تصنيفات آخرها «المزيد» · carousel «مقدمو خدمات مميزون» · «خدمات شائعة» · بطاقة الدعم |
| `/categories` | 08 + 07 | شبكة 3×3 للتصنيفات الرئيسية + «🔥 أكثر الخدمات طلبًا» مرقّمة 1–4 + بانر «جودة مضمونة» |
| `/categories/[slug]` | 07 | مهن التصنيف في شبكة 3×N + زر «عرض كل خدمات …» |
| `/services` | 09 | أيقونة التصنيف وعنوانه وعدّاده · بحث داخلي · `FilterBar` · بطاقات الخدمة · تمرير لا نهائي |
| `/providers/[id]` | 10 | مسار تنقّل · صورة كبيرة + عدّاد الصور + مشاركة/♡ · الهوية وشارة «موثّق» · شريط السعر المبدئي · 4 إحصاءات · 4 تبويبات · فوتر إجراءات ثابت |
| `/search` | مشتقّة (§0.1) | بحث مؤجَّل (debounce) بتبويبات الكل/الخدمات/المزوّدين |

### الـAPI (كل ما نصّت عليه الخطة، بلا زيادة)

`GET /providers` · `GET /providers/:id` · `GET /providers/:id/reviews` · `GET /services` · `GET /services/:id` · `GET /search`
(تُضاف إلى `/categories` و`/professions` من Phase 2/4.)

كلها تدعم: الفلاتر (المنطقة، السعر، التقييم، التصنيف، المهنة) · الترتيب الأربعة · الترقيم بـ`meta.hasMore`.

### المكوّنات

`ServiceCard` · `ProviderMiniCard` · `CategoryCard` · `CategoryTile` · `ProfessionCard` · `FilterBar` · `ProfileTabs` · `PromoBanner` · `SearchInput`/`SearchBox` · `Rating` · `MediaThumb` · `SupportCta` · `PopularServices` · `InfiniteSentinel` · `CatalogIcon` · `LinkButton` · هياكل تحميل جديدة.

### الفهارس

أُعيد بناء فهارس `ServiceProvider` و`Service` على شكل الاستعلام الفعلي بقاعدة **ESR** (مساواة ← ترتيب ← نطاق): حارس الظهور `{isActive, verification.status}` يتصدّر كل فهرس مركّب. الفهارس القديمة كانت تضع `isActive` **آخرًا**، فيسقط الفهرس عند أول فلتر.

---

## 2. Files changed

### ملفات جديدة (24)

**الخادم**
`src/server/repositories/discovery.repository.ts` · `src/server/services/discovery.service.ts`
`src/app/api/v1/providers/route.ts` · `providers/[id]/route.ts` · `providers/[id]/reviews/route.ts`
`src/app/api/v1/services/route.ts` · `services/[id]/route.ts` · `src/app/api/v1/search/route.ts`

**الواجهة**
`src/app/categories/page.tsx` · `src/app/categories/[slug]/page.tsx` · `src/app/services/page.tsx` · `src/app/providers/[id]/page.tsx` · `src/app/search/page.tsx`
`src/components/features/discovery/` (13 مكوّنًا) · `src/components/common/catalog-icon.tsx`
`src/lib/queries/discovery.ts` · `src/lib/cloudinary-url.ts`

**الاختبارات**
`tests/api/discovery.test.ts` · `tests/unit/discovery-query.test.ts` · `tests/unit/discovery-components.test.tsx` · `tests/unit/route-guard.test.ts`

### ملفات معدّلة (10)

`src/shared/schemas/catalog.schema.ts` (فلاتر الاكتشاف + مخططات البحث والتفاصيل) · `src/server/middleware/with-rate-limit.ts` (حدّ `SEARCH`) · `src/server/db/models/service.model.ts` و`service-provider.model.ts` (الفهارس) · `src/proxy.ts` (إصلاح مطابقة البادئة + مساحة العميل) · `src/components/ui/button.tsx` (`buttonClassName` + `LinkButton`) · `src/components/ui/skeleton.tsx` · `src/lib/query-keys.ts` · `src/app/home/page.tsx` · `vitest.config.mts`

---

## 3. Tests executed

```bash
npm run typecheck   # tsc --noEmit
npm run lint        # eslint --max-warnings=0
npm run check:constraints
npm test            # vitest run
npm run build
```

## 4. Test results

| المجموعة | العدد | النتيجة |
|---|---|---|
| `tests/api/discovery.test.ts` | 38 | ✅ |
| `tests/unit/discovery-query.test.ts` | 31 | ✅ |
| `tests/unit/discovery-components.test.tsx` | 28 | ✅ |
| `tests/unit/route-guard.test.ts` | 10 | ✅ |
| المجموعات السابقة (Phase 1–4) | 315 | ✅ |
| **الإجمالي** | **422** | **✅ 422/422** |

**التغطية الفعلية للمعايير المطلوبة في الخطة:**
- كل مجموعة فلاتر منفردة، ومجموعة مركّبة تجمع التصنيف + المنطقة + التقييم + السعر + الترتيب في استعلام واحد.
- الترتيبات الأربعة، مع التحقق من رتابة النتائج فعليًا لا من قبول المعامل فقط.
- الصفحات: صفحتان متتاليتان بلا تداخل ولا إسقاط، و`hasMore` مطابق.
- استبعاد غير المعتمدين: من القوائم، ومن التفاصيل (404)، ومن البحث، ومن البحث الجزئي، ومن التقييمات.
- وحدة: بناء الاستعلام من الفلاتر بلا قاعدة بيانات — بما فيها إثبات أن الحارس لا يسقط بأي تركيبة.

## 5. Build result

✅ `next build` ينجح. 40 مسارًا، منها 6 مسارات API جديدة و5 صفحات جديدة. لا تحذيرات.
`npm run check:constraints`: **152 ملفًا، صفر مخالفة** — لا دفع، لا خرائط/تتبّع، لا OTP، لا أسرار.

---

## 6. UI review — مقارنة بالصور 06–10

فُحصت الشاشات حيًّا على `375×812` و`320×700` ببيانات مبذورة حقيقية.

| العنصر | الصورة | الحالة |
|---|---|---|
| صف التصنيفات المصغّر (6 مربعات آخرها «المزيد») | 06 | ✅ بعد إصلاح |
| بانر `--brand-50` + رسم المدينة + نقاط carousel | 06 | ✅ |
| carousel المزوّدين بشارة التوثيق | 06 | ✅ |
| شبكة التصنيفات 3×3 بدائرة وعدّاد `X خدمة ›` | 08 | ✅ بعد إصلاح |
| «أكثر الخدمات طلبًا» مرقّمة 1–4 | 07 | ✅ |
| بانر «جودة مضمونة» | 07 | ✅ |
| بطاقة الخدمة: صورة 120 + Chip + ⭐ + 🛡 خبرة + 📍 + `بيدأ من` + «عرض التفاصيل» | 09 | ✅ |
| `FilterBar`: `📍 كل المناطق` · `السعر ⌄` · `التقييم ⌄` · `الترتيب ⌄` + `تصفية` | 09 | ✅ |
| ملف المزوّد بأقسامه السبعة و4 تبويبات | 10 | ✅ |
| الأرقام لاتينية داخل النص العربي | §1.2 | ✅ |
| صيغة المعدود العربية (`خدمتان` / `6 خدمات` / `14 خدمة`) | — | ✅ |
| RTL: زر الرجوع يسارًا، الشيفرون `‹` لليسار | §1.5 | ✅ |
| Responsive 320px بلا تمرير أفقي (`scrollWidth == clientWidth`) | — | ✅ |
| Empty / Error / Loading لكل قائمة | §1.6 | ✅ |

**انحرافان مقصودان عن الصور، مع سببهما:**

1. **دائرة بطاقة التصنيف 64px بدل 84px** — في شبكة 3×3 على عرض 375px يتبقى ~109px للبطاقة، فدائرة 84px + الحشو تتجاوز حدودها. النسب البصرية محفوظة.
2. **لوحة الفلاتر تُفتح تحت الشريط لا كقائمة عائمة** — القائمة العائمة في RTL تخرج عن حدود الشاشة على 375px.

---

## 7. Security review

| البند | الحالة |
|---|---|
| المزوّدون غير المعتمدين غير قابلين للاستعلام إطلاقًا | ✅ الحارس داخل **طبقة المستودع** لا الخدمات، فلا يمكن لمستدعٍ نسيانه. مُثبت باختبار وحدة (لا تركيبة فلاتر تُسقطه) وباختبارات API على القوائم والتفاصيل والبحث والتقييمات. |
| لا تسريب لهاتف/بريد المزوّد | ✅ `$project` **قائمة سماح** لا استبعاد؛ ولا تُضمّ مجموعة `users` في أي مسار عام عدا اسم صاحب التقييم. كل استجابة تُفحص نصًّا ضد `phone`/`email`/`+20…`/`@seed.local`. |
| لا تسريب لحقول التوثيق الداخلية | ✅ `verification.requestNumber` و`rejectionReason` و`userId` خارج قائمة السماح — مفحوصة نصيًّا في 4 اختبارات. |
| مزوّد قيد المراجعة → 404 لا 403 | ✅ لا نكشف حتى وجوده (ARCHITECTURE §7). |
| حدّ معدّل على البحث | ✅ `SEARCH = 30/دقيقة`، أضيق من القراءة العادية (120)، مُثبت باختبار يستنفده فعليًا حتى 429. |
| منع حقن NoSQL | ✅ كل معامل يمرّ بـZod `.strict()` (مفتاح غير معرّف = 400)، و`assertNoOperatorKeys` يرفض ما يبدأ بـ`$`. |
| منع حقن التعبير النمطي / ReDoS | ✅ `escapeRegex` على نص البحث قبل أي استخدام كنمط — `.*` يعيد صفر نتيجة لا كل النتائج (مؤكَّد حيًّا وباختبار). |
| التقييمات المخفية إداريًا | ✅ `isVisible = false` لا تُعاد — مُثبت بتقييم مخفي حقيقي في الاختبار. |
| لا إحداثيات ولا مسافات في أي فلتر | ✅ المنطقة نص من قائمة الفيوم الثابتة؛ منطقة خارجها = 400. اختبار يفحص أن ناتج بناء الاستعلام خالٍ من `lat|lng|coordinates|radius|geo`. |

---

## 8. Issues found

### 🔴 (1) حارس المسارات كان يطرد العميل من ملف مقدم الخدمة

`src/proxy.ts` استخدم `pathname.startsWith('/provider')` لعزل مساحة المزوّد. المسار الجديد `/providers/:id` (شاشة **عميل**، الصورة 10) يبدأ بهذه البادئة حرفيًا، فكان كل عميل مسجّل يُحوَّل إلى `/home` بدل رؤية الملف. الخطأ كان كامنًا في الشيفرة القائمة وأظهرته تسمية المسار الجديد.

### 🟠 (2) البحث العربي لا يجد البادئات

الفهرس النصي في MongoDB يطابق **الكلمات الكاملة**: «كهرب» لا تجد «كهربائي»، و«نظاف» لا تجد «تنظيف» — وهو بالضبط ما يكتبه المستخدم حرفًا حرفًا في حقل البحث. مُكتشَف بالتجريب الحيّ لا بالاختبارات.

### 🟡 (3) قرص «السعر» كان يقرأ «كل الأسعار» قبل اختيار أي فلتر

بحث التسمية طابق أول نطاق (حدّاه `undefined`) على الحالة الفارغة. التصميم (09) يعرض «السعر ⌄».

### 🟡 (4) قارئ الشاشة ينطق عدّاد التبويب ملتصقًا

خوارزمية حساب الاسم المتاح تقصّ الفراغات حول كل عنصر داخلي، فتصير «التقييمات128» مهما أُضيف من فراغ نصّي.

### 🟡 (5) الفوتر الثابت يغطّي آخر محتوى ملف المزوّد

`withBottomNav={false}` أزال الحشو السفلي بينما الفوتر الثابت ما زال يعوم فوق المحتوى.

### 🟡 (6) عنوان بطاقة الخدمة يُقصّ لسطر واحد + حشو ♡ غير مبرَّر

`line-clamp-1` مع `pe-7` دائم، فيُقصّ العنوان ويُترك فراغ لزر غير معروض.

### 🟡 (7) صيغة معدود خاطئة في عدّاد الخدمات

«4 خدمة متاحة» بدل «4 خدمات متاحة».

### 🟡 (8) مربعات التصنيفات والشبكات لم تطابق التخطيط المرسوم

الرئيسية كانت 3 أعمدة بمربعات كبيرة بدل صف من 6، والتصنيفات عمودين بدل 3×3.

---

## 9. Issues fixed — كلها (8/8)

| # | الإصلاح | التحقق |
|---|---|---|
| 1 | `matchesPrefix()` بحدود مقطعية (تطابق تام أو بادئة يتلوها `/`)، وقائمة `CUSTOMER_SPACE` صريحة، وإضافة شاشات الاكتشاف إلى المسارات المحمية | ملف اختبار جديد `route-guard.test.ts` — 10 اختبارات تغطي الزائر والعميل والمزوّد والإدارة، أوّلها هو الانحدار نفسه |
| 2 | خطة ثانية: عند صفر نتيجة يُعاد البحث بتطابق جزئي (`$regex`) على حقلي البطاقة، ونص البحث **مهرَّب بالكامل** | حيًّا: «كهرب» → 2 مزوّد + 4 خدمات · «نظاف» → 2 خدمة · `.*` → صفر (لا كل شيء) · 3 اختبارات API + 6 اختبارات وحدة |
| 3 | البحث عن تسمية النطاق مشروط بوجود فلتر سعر فعلي | اختبار مكوّن |
| 4 | `aria-label` صريح للتبويب ذي العدّاد: «التقييمات، 128» | اختبار مكوّن يفحص الاسم المتاح والنص المعروض معًا |
| 5 | `pb-32` على حاوية الملف يعادل ارتفاع الفوتر | لقطة: قسم «نبذة» ظاهر كاملًا |
| 6 | `line-clamp-2` والحشو مشروط بوجود `onToggleFavorite` | اختبار مكوّن + لقطة |
| 7 | `pluralizeAr(total, 'خدمة متاحة', 'خدمتان متاحتان', 'خدمات متاحة')` | لقطة حيّة |
| 8 | صف `grid-cols-6` في الرئيسية، وشبكة `grid-cols-3` في التصنيفات بدائرة 64px، والهياكل مطابقة للأبعاد الجديدة | لقطات مقارنة بالصور 06 و08 |

---

## 10. Remaining issues

| # | البند | الأثر | الجهة |
|---|---|---|---|
| 1 | البحث الجزئي (الخطة الثانية) يمسح المجموعة ولا يستخدم فهرسًا | لا أثر على المقاس الحالي؛ يحتاج فهرس n-gram أو Atlas Search عند نمو البيانات | Phase 10 |
| 2 | `$facet` يحسب الإجمالي بفرع لا يستفيد من الفهرس | مقبول حاليًا؛ البديل هو التخلّي عن `total` لصالح `hasMore` فقط | Phase 10 |
| 3 | ♡ (المفضلة) والمشاركة في الصور 09 و10 معروضة بلا سلوك | نظام المفضلة نفسه من نطاق Phase 9 | Phase 9 |
| 4 | «تواصل واتساب» في الصورة 10 معطّل بنص يوضّح السبب | **قرار أمني مقصود**: لا يخرج رقم هاتف من أي مسار عام؛ القناة تُفتح بعد قبول الطلب | Phase 7/8 |
| 5 | «اطلب الخدمة» يقود إلى `/orders/new` غير المبنية بعد | شاشة الطلب (الصورة 11) من نطاق Phase 7 | Phase 7 |
| 6 | معيار «≤ 400ms على Atlas» مقيس على `mongodb-memory-server` لا على Atlas حقيقي | الفهارس مبنية على شكل الاستعلام؛ يُعاد القياس عند توفّر عنقود Atlas | قبل الإطلاق |

---

## 11. Acceptance Criteria — التحقق

| # | المعيار | النتيجة |
|---|---|---|
| 1 | أول صفحة من أي قائمة ≤ 400ms بعد الفهارس | ⚠️ محليًا الاستجابة فورية والفهارس مبنية بقاعدة ESR على شكل الاستعلام؛ القياس على Atlas مؤجَّل (البند 6 أعلاه) |
| 2 | لا Layout shift | ✅ هياكل التحميل بنفس أبعاد البطاقات الحقيقية، و`MediaThumb` بأبعاد مثبتة على الحاوية قبل وصول الصورة |
| 3 | كل قائمة لها Empty / Error / Loading | ✅ في الرئيسية والتصنيفات والمهن والخدمات والبحث والتبويبات الأربعة |
| 4 | المزوّدون غير المعتمدين غير قابلين للاستعلام | ✅ على مستوى الـAPI لا الواجهة — 6 اختبارات |
| 5 | لا تسريب هاتف/بريد في القوائم العامة | ✅ قائمة سماح + فحص نصي لكل استجابة |
| 6 | حدّ معدل على البحث | ✅ 30/دقيقة، مُثبت حتى 429 |

**Definition of Done:** كل الميزات مكتملة ✅ · 422/422 اختبار ✅ · بناء نظيف ✅ · مقارنة بصرية بالصور 06–10 ✅ · مراجعة أمنية ✅ · تحقّق حيّ على المتصفح شمل الفلترة والبحث والتنقّل ✅ · تقرير المراجعة ✅

---

## 12. تشغيل المراجعة محليًا

```bash
node scripts/dev-db.mjs
```

ثم في نافذة أخرى (بعد ضبط `MONGODB_URI` في `.env.local` على الرابط المطبوع):

```bash
npm run db:seed -- --clear && npm run dev
```
