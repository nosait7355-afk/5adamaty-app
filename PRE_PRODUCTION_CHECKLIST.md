# PRE_PRODUCTION_CHECKLIST.md — بنود إلزامية قبل الإنتاج

> **حالة هذا الملف: ملزِم.** لا يُنشر التطبيق على الإنتاج قبل اجتياز كل بند هنا وتعليمه ✅ بتاريخ ومسؤول.
> اعتُمد هذا الملف بعد Phase 6 بقرار صريح من صاحب المشروع.

---

## 1. Cloudinary — اختبار الرفع الحقيقي (مؤجَّل من Phase 4 و6، **بدأ فعليًا في Phase 10**)

**السياق:** منطق الرفع مُغطّى بـ67 اختبارًا (Phase 4) + 38 اختبارًا لدورة التوثيق (Phase 6)، لكنها كانت **جميعًا بمحاكاة كاملة لاستجابات Cloudinary** حتى 2026-09-04.

> 🔴 **اكتُشف وأُصلح خلل حقيقي بهذا الاختبار الحي (2026-09-04):** `createUploadSignature`
> (`src/server/lib/cloudinary.ts`) كان يُدرج `max_bytes` ضمن المعاملات **الموقَّعة** ظنًّا
> أن Cloudinary يدعمه كقيد رفع — لكنه ليس معاملًا حقيقيًا في Cloudinary Upload API. أي
> رفع حقيقي كان سيُرفض دائمًا بـ`401 Invalid Signature` (السلسلة التي يحسبها Cloudinary
> فعليًا لا تتضمن `max_bytes` فتختلف عن توقيعنا). **كل الاختبارات المُحاكاة السابقة لم
> تكشفه لأنها لا تستدعي Cloudinary فعليًا.** أُزيل `max_bytes` من المعاملات الموقَّعة —
> حدّ الحجم يبقى مفروضًا بطبقتين مستقلتين لا علاقة لهما بالتوقيع: التحقق من `sizeBytes`
> المُعلَن قبل إصدار التوقيع، والتحقق من `asset.bytes` الحقيقي من Cloudinary بعد الرفع
> مباشرة قبل الحفظ (`upload.service.ts`). تم التحقق حيًّا: رفع فعلي → توقيع صالح →
> `200` من Cloudinary → حفظ ناجح → حذف ناجح (تأكّد غياب الأصل من Cloudinary مباشرة).

> 🔴 **خلل ثانٍ، اكتُشف وأُصلح بنفس الجلسة (2026-09-04) — التشخيص الأول كان خاطئًا
> وصُحِّح بعده مباشرة:** فتح مستند مرفوع (`GET /api/v1/provider/documents/[id]/url`)
> كان يعيد رابطًا **يفشل دائمًا** بـ`401 Unauthenticated access` من Cloudinary نفسه.
> السبب الحقيقي: `signDeliveryUrl` (`src/server/lib/cloudinary.ts`) كان يوقّع
> `public_id` وحده، بينما الرابط الفعلي يضع `public_id.format` بعد segment التوقيع
> `s--...--` — فالسلسلة الموقَّعة لا تطابق ما يحسبه Cloudinary من الرابط، فيُرفض دائمًا.
> **التشخيص الأول ظنّ أن `CLOUDINARY_AUTH_TOKEN_KEY` (`__cld_token__`، ميزة منفصلة
> تتطلب تذكرة دعم من Cloudinary) إلزامي — هذا غير صحيح.** تحقَّق لاحقًا من توثيق
> Cloudinary مباشرة: أصول `authenticated` تُفتح ذاتيًا بتوقيع `sign_url` عاديًا
> بـ`api_secret` وحده — بلا أي تدخّل من دعم Cloudinary. بعد تصحيح السلسلة الموقَّعة
> (إضافة `.format`)، عمل الفتح فورًا **بلا** `CLOUDINARY_AUTH_TOKEN_KEY` (تحقَّق حيًّا:
> `200` من Cloudinary، والصورة الفعلية عادت صحيحة بايتًا بايت). `CLOUDINARY_AUTH_TOKEN_KEY`
> يبقى كما كان أصلًا **تحسينًا اختياريًا** (يضيف انتهاء صلاحية زمني فقط) — التصنيف
> الأصلي في §3 البند 2 كان صحيحًا من البداية.

**التفعيل:** ضع `CLOUDINARY_CLOUD_NAME` و`CLOUDINARY_API_KEY` و`CLOUDINARY_API_SECRET` في `.env.local` (و`CLOUDINARY_AUTH_TOKEN_KEY` اختياريًا لاحقًا)، ثم:

```bash
npm run check:cloudinary
```

| # | البند | الحالة | ملاحظة |
|---|---|---|---|
| 1 | رفع **صورة حقيقية** إلى Cloudinary (صورة عامة: معرض المزوّد / مرفقات الطلب) | ⬜ | لم يُختبر بعد — `PROVIDER_GALLERY`/`ORDER_ATTACHMENT` لهما نفس مسار التوقيع المُصلَح، لكن لم يُجرَّب حرفيًا |
| 2 | رفع **مستندات حقيقية** (هوية / مؤهل / ترخيص) بوضع `authenticated` | ✅ 2026-09-04 | رُفع مستند `PERSONAL_PHOTO` فعليًا (`image/authenticated/`)، `secure_url` غير قابل للفتح العام كما هو متوقَّع |
| 3 | التحقق من **MIME / نوع الملف** برفع حقيقي | ⬜ | يشمل ملفًا مزوّر الامتداد (توقيع بايتات ≠ الامتداد) — يجب أن يُرفض |
| 4 | التحقق من **حدود حجم الملف** برفع حقيقي | ⬜ | ملف يتجاوز 5MB يُرفض، وملف عند الحد يُقبل — الدفاع طبقتان مستقلتان الآن لا `max_bytes` الموقَّع (انظر الخلل أعلاه) |
| 5 | **Signed URLs** — توليد رابط موقّت لمستند حقيقي وفتحه | ✅ 2026-09-04 | فُتح مستند فعلي (200) بتوقيع ذاتي (`api_secret` وحده) بعد إصلاح خلل السلسلة الموقَّعة الناقصة (`.format`) — انظر الخلل الثاني أعلاه. **لا يحتاج `CLOUDINARY_AUTH_TOKEN_KEY`** |
| 6 | **انتهاء صلاحية الـSigned URL** | ⬜ | يبقى تحسينًا اختياريًا كما صُنِّف أصلًا — يتطلب `CLOUDINARY_AUTH_TOKEN_KEY` (ميزة منفصلة، تذكرة دعم من Cloudinary)؛ بدونه الرابط صالح للفتح لكن بلا انتهاء زمني |
| 7 | **منع الوصول لمستند مستخدم آخر** برفع حقيقي | ⬜ | يجب أن يعيد 404 لا 403 (منع تعداد المعرّفات) |
| 8 | **حذف / استبدال الملفات** | ✅ 2026-09-04 (الحذف فقط) | حُذف المستند عبر `DELETE /api/v1/provider/documents/[id]`، وتأكَّد غياب الأصل من Cloudinary مباشرة (`GET /resources/...` → 404). **الاستبدال (إعادة رفع لنفس المتطلب) لم يُختبر بعد** |
| 9 | **التأكد أن MongoDB لا يحتوي أي ملف أو بيانات ثنائية** | ⬜ | فحص فعلي على قاعدة الإنتاج بعد رفع حقيقي — `metadata` فقط (`MediaRef`) بلا `Buffer` ولا Base64 |

**كيفية التحقق من البند 9 على قاعدة حقيقية:**

```bash
# لا يجب أن يعيد أي مستند
db.providerdocuments.find({ $or: [ { "media.data": { $exists: true } }, { "media.buffer": { $exists: true } } ] })
# ولا أي حقل من نوع binData في أي مجموعة
db.getCollectionNames().forEach(c => printjson({ c, binary: db[c].find({ $where: "Object.values(this).some(v => v instanceof BinData)" }).count() }))
```

> يوجد بالفعل اختبار سلبي دائم في `tests/db/models.test.ts` يمنع أي حقل ملفات في الـSchemas، لكن البند 9 يفحص **البيانات الفعلية** على الإنتاج لا التعريفات.

---

## 2. بيانات التطوير والمراجعة — ممنوعة على الإنتاج

**قاعدة ملزِمة:** كل حساب أو بيان استُخدم في المراجعة أثناء أي Phase هو **Development/Test فقط**، ولا يُنشأ ولا يُبذَر ولا يُرحَّل إلى الإنتاج بأي شكل.

**يشمل ذلك صراحةً:**

| البيان | المصدر | الحكم |
|---|---|---|
| `01011112222` / `Review12345` (عميل المراجعة) | أُنشئ يدويًا في قاعدة التطوير — Phase 5 | ❌ لا يُنقل |
| `admin@khadamaty.local` / `AdminReview123` (إدارة المراجعة) | أُنشئ يدويًا في قاعدة التطوير — Phase 6 | ❌ لا يُنقل |
| `mostafa@example.com` / `Provider12345` (مزوّد المراجعة، `SRV-2026-000001`) | أُنشئ عبر المعالج في قاعدة التطوير — Phase 6 | ❌ لا يُنقل |
| `provider1..22@seed.local` + `passwordHash: 'SEED_ONLY_NOT_A_REAL_HASH'` | `src/server/db/seed/data.ts` | ❌ لا يُبذَر على الإنتاج |
| مقدمو الخدمة الـ22 وخدماتهم الـ40 | سكربت البذر | ❌ بيانات عرض فقط |

**ما يُسمح ببذره على الإنتاج (بيانات مرجعية لا حسابات):** التصنيفات · المهن ومتطلبات مستنداتها · الأسئلة الشائعة · الإعدادات العامة. يلزم فصل سكربت بذر مرجعي مستقل قبل الإطلاق.

**الحواجز القائمة فعلًا:**
- `scripts/seed.mts` يرفض العمل عند `NODE_ENV=production` ويخرج برمز 1.
- `runSeed` يستخدم كلمات مرور وهمية لا تُنتج جلسة صالحة (`SEED_ONLY_NOT_A_REAL_HASH` ليس تجزئة argon2id، فالتحقق يفشل دائمًا).
- اختبار دائم يمنع تسرّب هذه الحسابات: انظر `tests/db/seed.test.ts`.

**⬜ قبل الإطلاق:** التحقق يدويًا من أن قاعدة الإنتاج لا تحوي أي بريد ينتهي بـ`@seed.local` أو `@example.com` أو `.local`، ولا أي مستخدم بحالة تجريبية.

---

## 3. بنود مؤجَّلة أخرى مرحّلة من المراحل السابقة

| # | البند | مصدره | الأثر |
|---|---|---|---|
| 1 | قياس زمن الاستجابة على **Atlas حقيقي** (معيار ≤ 400ms لأول صفحة) | Phase 5 | الفهارس مبنية بقاعدة ESR، لكن القياس تمّ على قاعدة في الذاكرة |
| 2 | ضبط `CLOUDINARY_AUTH_TOKEN_KEY` | Phase 4 | تحسين اختياري (يضيف انتهاء صلاحية زمني للروابط الموقَّعة) — يتطلب تذكرة دعم من Cloudinary لتفعيله على الحساب. بدونه فتح المستندات يعمل بشكل صحيح تمامًا (تحقَّق حيًّا 2026-09-04)، فقط بلا انتهاء زمني. انظر §1 |
| 3 | استبدال مخزن حدّ المعدّل بـRedis | Phase 1 | التنفيذ الحالي في الذاكرة — لا يصمد مع أكثر من نسخة خادم |
| 4 | تشديد CSP إلى nonce بدل `unsafe-inline` | Phase 1 | ✅ نُفِّذ في Phase 10 — `script-src` بلا `unsafe-inline` (`src/proxy.ts`) |
| 5 | سكربت تنظيف دوري للأصول اليتيمة في Cloudinary | Phase 4 | ملفات رُفعت ولم تُحفظ metadata لها |
| 6 | تنظيف دوري لمسودات التسجيل المهجورة (`DRAFT`) | Phase 6 | حسابات غير مكتملة تتراكم بلا حدّ |
| 7 | فهرس n-gram أو Atlas Search للبحث الجزئي العربي | Phase 5 | البديل الحالي يمسح المجموعة عند صفر نتيجة |

---

## 4. بروتوكول الاعتماد

لا يُعلَّم أي بند ✅ إلا بعد **تنفيذ فعلي موثَّق**، لا بمراجعة كود. يُسجَّل لكل بند: التاريخ · المنفِّذ · دليل التنفيذ (لقطة/سجل/ناتج أمر).
