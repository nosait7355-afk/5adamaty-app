# PHASE_03_REVIEW.md — مراجعة المرحلة الثالثة

> **الحالة النهائية: ✅ مكتملة (COMPLETED)**
> التاريخ: 2026-09-03 · المرجع: `PROJECT_PLAN.md § Phase 3`

---

## 1. What was implemented

### 1.1 الأوليات الأمنية
| الملف | المحتوى |
|---|---|
| `password.ts` | **argon2id** (m=19MiB · t=2 · p=1 — توصية OWASP) · **تجزئة وهمية** تُستهلك عند عدم وجود المستخدم لمنع تعداد الحسابات بقياس الزمن · توليد توكنات عشوائية 32 بايت · `hashToken` (SHA-256) · مقارنة ثابتة الزمن |
| `jwt.ts` | Access 15 دقيقة · Refresh 30 يومًا · **جمهوران منفصلان** (`access`/`refresh`) بسرَّين مختلفين — توكن أحدهما لا يُقبل مكان الآخر · مبنية على `jose` لتعمل في Edge |
| `cookies.ts` | `httpOnly` + `secure` (إنتاج) + `sameSite=lax` + `path=/` · «تذكرني» يحوّل كوكي التحديث من كوكي جلسة إلى دائمة |
| `csrf.ts` | فحص `Origin` (ثم `Referer`) على كل طلب مُغيِّر · allowlist يشمل مصادر Capacitor لأندرويد |
| `email.ts` | واجهة إرسال قابلة للتوصيل — **لا يوجد مزوّد بريد مُعدّ بعد** (تفصيل في §8) |

### 1.2 خدمة المصادقة
`auth.service.ts` — 7 عمليات:
- **التسجيل** — يرفض الهاتف/البريد المكرر برسائل مميّزة، يجزّئ كلمة المرور، يصدر جلسة.
- **الدخول** — بالهاتف أو البريد، **رسالة فشل موحّدة**، قفل الحساب 15 دقيقة بعد 5 محاولات، تنظيف الجلسات المنتهية.
- **التحديث** — تدوير كامل + **كشف إعادة الاستخدام**.
- **الخروج** — جلسة واحدة أو كل الأجهزة.
- **إعادة التعيين** — توكن 32 بايت مُجزّأ، صالح 30 دقيقة، مرة واحدة، يُبطل كل الجلسات.

### 1.3 الـAPI (7 مسارات)
`POST /auth/register` · `/auth/login` · `/auth/refresh` · `/auth/logout` · `/auth/forgot-password` · `/auth/reset-password` · `GET /auth/me`
كلها تمرّ بالسلسلة: `withErrorHandler → assertSameOrigin → enforceRateLimit → validateBody → service`.

### 1.4 حارس المسارات
`src/proxy.ts` — يعمل على الحافة قبل تحميل أي صفحة:
- زائر → صفحة محمية: تحويل إلى `/login?next=<الوجهة>`.
- مسجّل → صفحة ضيوف: تحويل لصفحته حسب الدور.
- عزل المساحات: `/admin` للإدارة · `/provider` ليس للعميل · `/home` ليس للمزوّد.

> ⚠️ الحارس **للتوجيه فقط**. التفويض الحقيقي في كل endpoint عبر `requireAuth`/`requireRole`/`assertOwnership` — الـAPI قابلة للاستدعاء مباشرة بلا مرور عليه.

### 1.5 الشاشات
| الشاشة | المرجع | الحالة |
|---|---|---|
| Splash | الصورة 01 | ✅ موجتان + اللوجو + الاسم بين خطين + رسم المعالم + Spinner، وتوجيه حسب الجلسة والدور |
| اختيار نوع الحساب | الصورة 02 | ✅ بطاقتان، المنتقاة بحد أزرق + زاوية مائلة + ✓ · بطاقة «آمن وموثوق» |
| تسجيل الدخول | الصورة 03 + تبويبات 04 | ✅ تبويبا الهاتف/البريد · مفتاح +20 · 👁 · تذكرني · نسيت كلمة المرور |
| إنشاء حساب | الصورة 05 | ✅ الحقول الستة + الموافقة على الشروط |
| نسيت كلمة المرور | مشتقّة | ✅ يبررها رابط في الصورة 03 |
| كلمة مرور جديدة | مشتقّة | ✅ وجهة رابط البريد |

**عنصران بصريان جديدان:** `CitySkyline` (رسم SVG لمعالم الفيوم: برج الساعة، المسجد بقبته ومئذنته، المباني، النخيل، القوارب، الجسر) و`BrandWave`.

---

## 2. Files changed

**جديد (21):**
```
src/server/lib/       password.ts · jwt.ts · cookies.ts · csrf.ts · email.ts
src/server/repositories/user.repository.ts
src/server/services/auth.service.ts
src/shared/schemas/auth.schema.ts
src/app/api/v1/auth/  register · login · refresh · logout · me
                      forgot-password · reset-password        (7 مسارات)
src/proxy.ts
src/app/              role-select · login · register · forgot-password · reset-password
                      home (مؤقتة) · provider/pending-review (مؤقتة)
src/components/features/auth/  city-skyline.tsx · auth-shell.tsx
src/components/layout/brand-icon.tsx
src/lib/queries/auth.ts
scripts/smoke-auth.mjs
tests/api/auth.test.ts · tests/unit/auth-guards.test.ts
```

**معدّل (7):** `src/app/page.tsx` (صار Splash) · `with-auth.ts` (الجلسة الحقيقية) · `env.ts` (فرض الأسرار في الإنتاج) · `user.repository.ts` · `seed.ts` (`trusted()`) · `vitest.config.mts` (بيئتان) · `package.json`

---

## 3. Tests executed & results

```
Test Files  9 passed (9)
     Tests  248 passed (248)
```

| الملف | العدد | التغطية |
|---|---|---|
| `api/auth.test.ts` | **37** | التسجيل (6) · الدخول (6) · القفل (3) · تنظيف الجلسات (1) · **التدوير وكشف إعادة الاستخدام (6)** · الخروج (3) · إعادة التعيين (7) · JWT (5) |
| `unit/auth-guards.test.ts` | **32** | argon2id (5) · التوكنات (3) · **CSRF (7)** · استخراج الجلسة (6) · حُرّاس الأدوار (5) · تطبيع المعرّف (4) · محتوى التوكن (2) |
| باقي الملفات | 179 | Phase 1 و2 |

### فحص الدخان الحيّ (`scripts/smoke-auth.mjs`)
شُغّل على خادم حقيقي بقاعدة بيانات حقيقية — **18/18 نجحت**:
```
✅ التسجيل ينجح (201)            ✅ الدخول بالبريد ينجح
✅ لا يعيد passwordHash          ✅ التحديث ينجح
✅ كوكي الوصول httpOnly          ✅ توكن التحديث يتغيّر (rotation)
✅ كوكي التحديث httpOnly         ✅ إعادة استخدام التوكن القديم مرفوضة (401)
✅ SameSite=Lax                  ✅ كل الجلسات أُبطلت بعد كشف إعادة الاستخدام
✅ Path=/                        ✅ زائر يُوجَّه من /home إلى /login
✅ me يعيد المستخدم              ✅ geolocation معطّلة
✅ me لا يسرّب حقولًا حسّاسة      ✅ payment معطّلة
✅ يرفض المصدر الخارجي (403)     ✅ رسالة موحّدة لحساب غير موجود ولكلمة مرور خاطئة
```

### تحقق تفاعلي في المتصفح
سُجّل حساب من شاشة التسجيل فعليًا:
- التسجيل → توجيه تلقائي إلى `/home` ✅
- `document.cookie` فارغة (httpOnly يعمل) ✅ · `localStorage` صفر مفاتيح ✅
- مسجّل يفتح `/login` → يُوجَّه إلى `/home` ✅
- كلمة مرور خاطئة → «رقم الهاتف أو البريد الإلكتروني أو كلمة المرور غير صحيحة» ✅
- بعد الخروج: `/auth/me` = 401 · `/home` → `/login?next=%2Fhome` ✅

---

## 4. Build result

| الفحص | النتيجة |
|---|---|
| `tsc --noEmit` | ✅ PASS |
| `eslint --max-warnings=0` | ✅ PASS — 0/0 |
| `check:constraints` | ✅ PASS — **107 ملفًا، 0 مخالفة** |
| `vitest run` | ✅ PASS — 248/248 |
| `smoke-auth` | ✅ PASS — 18/18 |
| `next build` | ✅ PASS — 23 مسارًا + Proxy |
| `npm audit` | ✅ **0 vulnerabilities** |

---

## 5. UI review

مراجعة بصرية على 375px مقابل الصور 01، 02، 03، 05:

| البند | النتيجة |
|---|---|
| Splash: موجتان + لوجو + اسم بين خطين + معالم + Spinner | ✅ |
| Role Select: البطاقة المنتقاة بحد أزرق + زاوية مائلة + ✓ | ✅ |
| ترتيب البطاقتين RTL (مستخدم يمينًا) | ✅ |
| بطاقة «آمن وموثوق» | ✅ |
| Login: تبويبان + مفتاح +20 + 👁 + تذكرني | ✅ |
| Login: بطاقة «ليس لديك حساب؟ إنشاء حساب جديد» | ✅ |
| Register: الحقول الستة + `*` الحمراء + الموافقة | ✅ |
| رسم معالم الفيوم أسفل الشاشات | ✅ |
| زر الرجوع في **اليسار** (القرار المعتمد) | ✅ |
| تجاوز أفقي | ✅ 0px |

---

## 6. Security review

| البند | النتيجة | التحقق |
|---|---|---|
| argon2id لا تخزين نصي | ✅ | اختبار يؤكد `$argon2id$` |
| salt عشوائي لكل تجزئة | ✅ | اختبار |
| منع تعداد الحسابات | ✅ | رسالة موحّدة + تجزئة وهمية — مؤكّد حيًّا |
| قفل بعد 5 محاولات | ✅ | 3 اختبارات |
| كوكيز httpOnly | ✅ | مؤكّد حيًّا: `document.cookie` فارغة |
| صفر توكن في localStorage | ✅ | مؤكّد حيًّا: 0 مفاتيح |
| فصل جمهور التوكنين | ✅ | 4 اختبارات |
| تدوير + كشف إعادة الاستخدام | ✅ | 6 اختبارات + فحص حيّ |
| CSRF | ✅ | 7 اختبارات + رفض حيّ (403) |
| إعادة التعيين: مُجزّأ · 30 دقيقة · مرة واحدة | ✅ | 5 اختبارات |
| تغيير كلمة المرور يُبطل كل الجلسات | ✅ | اختبار |
| توكن الوصول بلا بيانات حسّاسة | ✅ | اختبار يفحص المطالبات |
| Rate limit 5/دقيقة على المصادقة | ✅ | |
| **صفر OTP · صفر Social Login** | ✅ | فحص القيود على 107 ملفات |

---

## 7. Issues found → Issues fixed

### 🔴 (1) بيئة jsdom تكسر jose — 28 اختبارًا فاشلًا
`TextEncoder` في jsdom يُنتج `Uint8Array` من realm مختلف، فيرفضه jose برسالة محيّرة: «Received an instance of Uint8Array» بينما هو يطلب Uint8Array.
**الإصلاح:** فصل بيئتَي الاختبار في `vitest.config.mts` — `jsdom` للمكوّنات و`node` لاختبارات السيرفر وقاعدة البيانات.

### 🔴 (2) `sanitizeFilter` يجرّد المُعاملات الشرعية — 5 اختبارات + فشل البذر
الحماية من حقن NoSQL (المفعّلة في Phase 2) جرّدت `$gt` من استعلام إعادة التعيين و`$in` من مرشّح البذر، فانهارا بـ`CastError`.
**الإصلاح:** `mongoose.trusted()` على المُعاملات القادمة من السيرفر حصرًا، مع تعليق يمنع استخدامها مع أي قيمة أصلها من الطلب.
**تحقق إضافي:** فحصت كل الملفات بحثًا عن مُعاملات غير محمية — الوحيد المتبقي `$pull` داخل **مستند التحديث** لا المرشّح، فلا يمسّه `sanitizeFilter`. أضفت اختبارًا يثبت أن تنظيف الجلسات المنتهية يعمل فعلًا.

### 🟠 (3) اسم العلامة مكرر — رصدته المراجعة البصرية
`public/logo.png` قفل علامة كامل (أيقونة + «خدماتي الفيوم» + الشعار)، والشاشات ترسم الاسم نصًا فوقه — فظهر مرتين.
**الإصلاح:** مكوّن `BrandIcon` يقصّ الأيقونة وحدها بحاوية `overflow-hidden` بدل توليد ملف ثانٍ. النسبة ضُبطت بصريًا على 0.66 بعد أن أبقت 0.72 نقاط حروف «خدماتي» ظاهرة.

### 🟠 (4) رقم الهاتف يُعرض مقلوبًا — رصدته المراجعة البصرية
`010 1234 5678` كان يُعرض `5678 1234 010`: محرك BiDi يعيد ترتيب مجموعات الأرقام في حقل RTL.
**الإصلاح:** `dir="ltr"` + `text-end` على حقول الهاتف والبريد — ترتيب صحيح مع بقاء المحاذاة يمينًا كما في التصميم.
> ثالث خطأ BiDi في المشروع (بعد `formatExperience` في Phase 1 و`FileHint` في Phase 2). النمط واضح: **أي نص لاتيني داخل سياق عربي يحتاج عزلًا صريحًا**.

### 🟡 (5) إيموجي العلم يظهر كحروف «EG»
ويندوز لا يرسم رموز الأعلام (regional indicators).
**الإصلاح:** حُذف الإيموجي وبقي `+20` نظيفًا.

### 🟡 (6) تعارض أنواع react-hook-form
`.default()` في Zod تجعل الحقل اختياريًا في نوع الإدخال وإلزاميًا في الإخراج، فرفض RHF الـresolver.
**الإصلاح:** تصدير نوعين لكل مخطط (`*FormValues` = `z.input` و`*Input` = `z.output`) واستخدام صيغة الأنواع الثلاثة في `useForm`.

### 🟡 (7) `Algorithm` تعداد ambient const
`isolatedModules` يمنع الوصول إليه وقت التشغيل.
**الإصلاح:** كتابة القيمة رقمًا (`2` = Argon2id) مع تعليق يوضح السبب.

### 🟡 (8) اصطلاح `middleware` مهمل في Next 16
**الإصلاح:** إعادة تسمية `src/middleware.ts` → `src/proxy.ts` وتغيير اسم الدالة المصدَّرة. أُعيد التحقق من عمل الحارس بعد التغيير (307 → `/login?next=%2Fhome`).

---

## 8. Remaining issues

### ⚠️ (1) لا يوجد مزوّد بريد — **مؤجَّل إلى Phase 10 بقرار معتمد**
`sendEmail` تسجّل الرسالة في السجل ولا ترسل شيئًا. في التطوير يظهر رابط إعادة التعيين في سجل الخادم، وفي الإنتاج **لن يصل المستخدم أي بريد**.

**المنطق كامل ومختبَر** (توكن مُجزّأ، صلاحية 30 دقيقة، استخدام واحد، إبطال كل الجلسات) — الناقص هو التوصيل فقط، وهو مقصور على ملف واحد `src/server/lib/email.ts` بواجهة ثابتة لن تتغيّر.

**القرار:** عُرض السؤال على صاحب المشروع و**اعتُمد تأجيل توصيل البريد إلى Phase 10** مع اختيار المزوّد هناك.

**الأثر حتى ذلك الحين:**
- ✅ التطوير والاختبار غير متأثرين — الرابط يظهر في السجل.
- ⛔ **لا يجوز إطلاق الإنتاج قبل تنفيذ هذا البند**، وإلا لن يستطيع أي مستخدم استعادة حسابه.
- تُسجَّل حالة الإرسال في `EmailResult.loggedOnly` ويُكتب تحذير في سجل الإنتاج عند كل محاولة إرسال فاشلة.

### (2) بنود مؤجّلة لمراحل لاحقة
| # | البند | الجهة |
|---|---|---|
| 1 | `/home` و`/provider/pending-review` صفحتان مؤقتتان | Phase 5 و6 |
| 2 | `/register/provider` غير موجودة — زر «متابعة» لمقدم الخدمة يقود لـ404 | Phase 6 |
| 3 | تسجيل دخول Admin بلا شاشة مخصّصة | Phase 10 |
| 4 | تحديد المعدّل في الذاكرة — Redis للنشر متعدد النسخ | Phase 10 |
| 5 | لا توجد صفحتا `/terms` و`/privacy` (روابط في التسجيل) | Phase 9 |
| 6 | تجديد التوكن التلقائي عند 401 غير مفعّل في `api-client` | Phase 5 |

---

## 9. Acceptance Criteria — التحقق

| # | المعيار | النتيجة |
|---|---|---|
| 1 | لا توكن في localStorage | ✅ مؤكّد حيًّا في المتصفح — 0 مفاتيح |
| 2 | لا وصول لصفحة محمية بلا جلسة | ✅ 307 → `/login?next=` |
| 3 | إعادة استخدام refresh token تُبطل كل الجلسات | ✅ 6 اختبارات + فحص حيّ |
| 4 | Splash يوجّه للوجهة الصحيحة في الحالات الأربع | ✅ عميل/مزوّد معتمد/مزوّد قيد المراجعة/زائر |
| 5 | لا OTP ولا Social Login | ✅ فحص آلي على 107 ملفات |

**Definition of Done:** كل الميزات موجودة ✅ · 248/248 اختبار ✅ · بناء نظيف ✅ · lint وtypecheck نظيفان ✅ · مراجعة UI مقابل الصور ✅ · مراجعة أمنية ✅ · فحص دخان حيّ 18/18 ✅ · تقرير المراجعة ✅

---

## 10. كيفية التشغيل

```bash
npm install
```

`.env.local` — الأسرار **إلزامية** الآن:
```bash
node -e "console.log('JWT_ACCESS_SECRET='+require('crypto').randomBytes(48).toString('base64url'))"
node -e "console.log('JWT_REFRESH_SECRET='+require('crypto').randomBytes(48).toString('base64url'))"
```

**تطوير بلا Atlas:**
```bash
npm run db:dev
```
ثم في نافذة أخرى مع `MONGODB_URI=mongodb://127.0.0.1:27077/`:
```bash
npm run db:seed && npm run dev
```

**فحص تدفق المصادقة على خادم يعمل:**
```bash
node scripts/smoke-auth.mjs http://localhost:3000
```
