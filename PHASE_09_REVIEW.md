# مراجعة المرحلة 9 — الحساب والإشعارات والتقييمات والمراسلة والمساعدة

**التاريخ:** 2026-09-04
**الحالة النهائية:** ✅ مكتملة — كل الفحوص خضراء
**البروتوكول:** لا انتقال إلى Phase 10 إلا بعد موافقتك الصريحة.

---

## 1. ما تم تنفيذه

المرحلة 9 تغلق الحلقة حول الطلب: بعد أن ينتهي العمل في Phase 7/8، يحتاج المستخدم أن
يُخطَر، ويقيّم، ويتواصل، ويدير حسابه، ويجد إجابة. خمس قدرات:

### أ) الإشعارات (الصورة 15)
- تبويبات «الكل / طلباتي / العروض» بعدّادات حيّة، تجميع زمني («اليوم» / «أمس» / تاريخ)،
  أيقونة ولون لكل نوع، نقطة غير مقروء + خلفية مظللة، «تعليم الكل كمقروء».
- شارة العدد في الهيدر وفي شريط التنقل السفلي، تُحدَّث من `unread-count`.
- **معيار القبول المحقق:** كل انتقال حالة في Phase 7/8 يولّد الإشعار الصحيح للطرف
  الصحيح. تحقق حيًّا: الطلب #1001 مرّ بأربع مراحل ← أربعة إشعارات مطابقة.

### ب) التقييمات
- شاشة `/orders/[id]/review`: خمس نجوم بوصف نصي («ممتازة» عند 5)، تعليق اختياري 500 حرف.
- **أربعة شروط على الخادم** (لا على الواجهة): الطلب للعميل نفسه · حالته `COMPLETED` ·
  لم يُقيَّم من قبل · التقييم عدد صحيح 1–5. مع التقاط `duplicate key` كحزام ثانٍ.
- `refreshProviderRating()` **يعيد الحساب من مصدر الحقيقة** (متوسط المراجعات الظاهرة)
  ولا يستعمل `$inc` — فلا ينحرف العدّاد أبدًا عن البيانات.

### ج) المراسلة
- محادثة واحدة لكل طلب (`orderId` فريد + upsert)، عدّاد غير مقروء لكل طرف.
- **قفل التواصل:** العميل لا يرى بيانات المزوّد ولا يفتح محادثة قبل قبول الطلب (403)؛
  المزوّد يرى العميل من `NEW` لأن العميل هو من بادر. تُعاد التحقق **مع كل رسالة** لا عند
  فتح المحادثة فقط.
- شاشتان مشتركتان بين الدورين (`messages-list-screen` / `thread-screen`) بمتغيّر `variant`.

### د) الحساب والعناوين والمفضلة (الصورتان 16 و17)
- `/account`: بطاقة الهوية، إحصاءات، أربع بطاقات لحالات الطلبات، أدوات، خروج.
  **«وسائل الدفع — »** مع الملاحظة: «الدفع كاش مباشرة لمقدم الخدمة — لا يوجد دفع
  إلكتروني في التطبيق». (معيار قبول صريح للمرحلة.)
- `/account/addresses`: عناوين **نصية بحتة**، أول عنوان افتراضي تلقائيًا، ترقية عنوان آخر
  عند حذف الافتراضي، حد أقصى 10، وملاحظة ظاهرة: «لا يستخدم التطبيق خرائط ولا يتتبّع موقعك».
- `/account/favorites`: تمرّ عبر **مستودع الاكتشاف**، فيسري عليها حارس الظهور — مزوّد
  فقد اعتماده يختفي من المفضلة كما يختفي من البحث.

### هـ) مركز المساعدة (الصورة 18)
- بحث في الأسئلة، أربع قنوات تواصل (`tel:` / `mailto:` / `wa.me` — روابط نظام لا تكاملات)،
  Accordion، «مفيد / غير مفيد»، مواضيع، نموذج تواصل، وتنبيه الدفع الكاش.

---

## 2. الملفات المضافة والمعدّلة

**مضاف — الخادم**
- `src/shared/schemas/account.schema.ts`
- `src/server/repositories/account.repository.ts`
- `src/server/repositories/messaging.repository.ts`
- `src/server/services/account.service.ts`
- `src/server/services/messaging.service.ts`
- 14 مسار تحت `/api/v1/`: `notifications`, `notifications/[id]`, `notifications/read-all`,
  `notifications/unread-count`, `orders/[id]/review`, `orders/[id]/thread`, `threads`,
  `threads/[id]/messages`, `me/profile`, `me/account`, `me/addresses(/[id])`,
  `me/favorites`, `faqs`, `faqs/[id]/feedback`, `support/contact`

**مضاف — الواجهة**
- شاشات: `/notifications` · `/account` · `/account/addresses` · `/account/favorites` ·
  `/help` · `/orders/[id]/review` · `/messages` · `/messages/[id]` ·
  `/provider/messages` · `/provider/messages/[id]`
- مكوّنات مشتركة: `src/components/features/messaging/{messages-list-screen,thread-screen}.tsx`
- `src/lib/queries/account.ts` (كل خطّافات المرحلة)

**معدّل**
- `src/proxy.ts` — إضافة `/messages` و`/notifications` و`/account` إلى المسارات المحمية،
  واستثناء `/messages` من `CUSTOMER_SPACE` لأنها مشتركة بين الدورين.
- `src/server/db/seed.ts` — بذر الأسئلة الشائعة.
- خمسة مستودعات — استبدال خيار Mongoose المهجور `new: true` بـ `returnDocument: 'after'`.

---

## 3. الاختبارات

| الملف | التغطية |
|---|---|
| `tests/api/account.test.ts` (42 اختبارًا) | الإشعارات · التقييم · العناوين · المفضلة · حسابي والمساعدة · المراسلة · **E2E: إكمال ← إشعار ← تقييم ← ظهوره في ملف المزوّد** |
| `tests/unit/validation.test.ts` | مخططات المرحلة الصارمة، والعنوان النصي البحت |

**النتيجة النهائية (بعد إصلاح التهجير):**

```
Test Files  21 passed (21)
Tests       667 passed (667)
```

`npm run verify` → **EXIT=0**: typecheck ✅ · lint (`--max-warnings=0`) ✅ ·
check:constraints ✅ · test 667/667 ✅ · build ✅ (`Compiled successfully`, 86 مسارًا).

---

## 4. المراجعة الحيّة للواجهة

شُغّل الخادم على بيانات مبذورة وجُرّبت الحلقة كاملة بحسابين حقيقيين
(عميل `01033334444` · مزوّد `plumber@dev.local`):

| الشاشة | النتيجة |
|---|---|
| `/notifications` (15) | ✅ التبويبات 4/3/0، التجميع الزمني، الشارة 4 في الهيدر والشريط السفلي |
| `/orders/[id]/review` | ✅ الإرسال معطّل قبل اختيار نجمة · «ممتازة» عند 5 · «شكرًا لتقييمك» |
| `/account` (16) | ✅ «وسائل الدفع —» + ملاحظة الكاش |
| `/account/addresses` (17) | ✅ البطاقة، شارة «محدّد»، الحذف، ملاحظة «بلا خرائط» |
| `/account/favorites` | ✅ المزوّد بتقييمه المعاد حسابه 5.0 (1) |
| `/help` (18) | ✅ البحث، القنوات الأربع، Accordion |
| `/messages/[id]` | ✅ الرسالة والوقت ورابط الطلب #1001 وصندوق الإرسال |

---

## 5. المراجعة الأمنية

- **IDOR:** كل استعلام في `account.repository` مقيَّد بـ `userId` من الجلسة — لا يُقرأ
  معرّف المالك من الطلب أبدًا. الوصول لمورد الغير يعيد **404** لا 403 (لا يكشف الوجود).
- **Mass-assignment:** `updateProfileSchema` لا يقبل `role` ولا `status` ولا `phone`؛
  كل المخططات `.strict()`.
- **حقن NoSQL:** بحث الأسئلة يهرّب الـregex ويلفّ كل شرط حقل على حدة بـ
  `mongoose.trusted()` — لا يُلف مصفوفة `$or` كاملة (لا تعمل).
- **قفل التواصل** يُعاد فحصه مع كل رسالة، لا عند فتح المحادثة فقط.
- **القيود غير القابلة للتفاوض:** `check:constraints` أخضر على 225 ملفًا — لا دفع إلكتروني،
  لا خرائط/إحداثيات، لا OTP/دخول اجتماعي، لا أسرار في الواجهة.

---

## 6. المشكلات التي ظهرت وعولجت

| # | الخطورة | المشكلة | العلاج |
|---|---|---|---|
| 1 | 🟠 | بحث الأسئلة الشائعة يعيد 500: `sanitizeFilter` يجرّد `$regex` فينتج CastError | لفّ كل شرط حقل منفردًا بـ `mongoose.trusted({ $regex, $options })` |
| 2 | 🟡 | أخطاء lint `set-state-in-effect` في شاشتي المرحلة | استبدالها بـ `useRef` حارس (`lastCountRef`) وضبط الحالة في مرحلة العرض |
| 3 | 🟡 | تحذيرات Mongoose المهجورة (`new: true`) تلوّث مخرجات الاختبارات في 9 مواضع | استبدالها بـ `returnDocument: 'after'` — التحذيرات الآن **صفر** |

---

## 7. ملاحظات مفتوحة (ليست عيوبًا في الكود)

1. **تعارض في بيانات البذر:** المزوّدون المبذورون يحملون `ratingAvg`/`ratingCount` وهميين
   بلا مستندات `Review` تسندهما. أول تقييم حقيقي يعيد الحساب من المصدر (4.7/75 ← 5.0/1).
   هذا **السلوك الصحيح** لـ`refreshProviderRating`، لكن يُستحسن لاحقًا أن يبذر السكربت
   مراجعات مطابقة أو يبدأ من صفر.
2. **جانب فقاعات المحادثة:** لا توجد صورة مرجعية لشاشة المحادثة (شاشة مشتقّة). اخترنا:
   رسائلي في اتجاه بداية القراءة، رسائل الطرف الآخر في نهايتها، مع تمييز لوني إضافي.
   قابلة للعكس بسطر واحد إن رغبت في محاكاة ترتيب واتساب العربي.
3. **تحديث المحادثة** بالسحب الدوري كل 15 ثانية (`refetchInterval`) لا بـ WebSocket — كافٍ
   للنطاق الحالي ومذكور صراحة.
4. **بنود ما قبل الإنتاج** في `PRE_PRODUCTION_CHECKLIST.md` تبقى ملزمة كما اعتُمدت.

---

## 8. الحالة

**Phase 9 — مكتملة ✅**
667/667 اختبارًا · lint نظيف · القيود خضراء · البناء ناجح · الشاشات السبع مراجَعة حيًّا.

**في انتظار موافقتك للانتقال إلى Phase 10.**
