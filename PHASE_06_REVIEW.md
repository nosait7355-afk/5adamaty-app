# PHASE_06_REVIEW.md — تسجيل مقدم الخدمة والتوثيق

> المرجع: [`PROJECT_PLAN.md § Phase 6`](PROJECT_PLAN.md) · [`UI_ANALYSIS.md`](UI_ANALYSIS.md) · [`ARCHITECTURE.md`](ARCHITECTURE.md)
> الحالة: ✅ **مكتملة** · 499/499 اختبار ناجح · بناء نظيف · دورة حياة كاملة مُختبَرة حيًّا على المتصفح

---

## 1. What was implemented

### المعالج والشاشات (19 · 20 · 21 · 22 · 23 + شاشة الإدارة)

| المسار | الصورة | المحتوى |
|---|---|---|
| `/register/provider` | 19–22 | معالج 4 خطوات بحفظ تلقائي للمسودة، تحقق لكل خطوة، وشاشة مراجعة بأزرار «تعديل» لكل قسم |
| `/provider/pending-review` | 23 | بطاقة الحالة + رقم `SRV-YYYY-NNNNNN` + تاريخ الإرسال + خطوات المراجعة الأربع + معلومات هامة |
| `/admin/providers` | مشتقّة (§0.1) | طابور المراجعة بتبويبات الحالات + لوحة مراجعة لكل طلب + قرارات الإدارة الثلاثة |

### دورة حياة الطلب

```
DRAFT ──(إرسال + مستندات مكتملة)──► PENDING_REVIEW ──► APPROVED   → يظهر في البحث ويستقبل طلبات
                       ▲                              ├► REJECTED
                       └──────(تعديل وإعادة إرسال)────┴► RESUBMISSION_REQUIRED
```

**لماذا `DRAFT` حالة جديدة:** المستندات تُرفع إلى Cloudinary بوضع مقيّد وتُربط بـ`providerId`، فلا يمكن رفعها قبل وجود حساب. لذلك يُنشأ الحساب عند الانتقال من الخطوة 2 إلى 3، وينتهي في `DRAFT` — لا يظهر في طابور الإدارة، ولا يُحسب في مدة الاستجابة، ولا يقبل أي قرار (409).

### الـAPI (كل ما نصّت عليه الخطة)

`POST /auth/register-provider` · `GET /provider/profile` · `PATCH /provider/profile` · `POST /provider/verification/submit` · `GET /admin/providers` · `GET /admin/providers/:id` · `PATCH /admin/providers/:id/verification`

---

## 2. Files changed

### ملفات جديدة (13)

**الخادم**
`src/shared/schemas/provider.schema.ts` · `src/server/repositories/provider.repository.ts` · `src/server/services/provider.service.ts`
`src/app/api/v1/auth/register-provider/route.ts` · `provider/profile/route.ts` · `provider/verification/submit/route.ts`
`src/app/api/v1/admin/providers/route.ts` · `admin/providers/[id]/route.ts` · `admin/providers/[id]/verification/route.ts`

**الواجهة**
`src/app/register/provider/page.tsx` · `src/app/admin/providers/page.tsx`
`src/components/features/provider/basic-info-step.tsx` · `profession-step.tsx` · `review-step.tsx`
`src/lib/queries/provider.ts`

**الاختبارات**
`tests/api/provider-registration.test.ts` · `tests/unit/provider-registration.test.ts`

### ملفات معدّلة (8)

`src/shared/constants/roles.ts` (حالة `DRAFT`) · `src/server/db/models/user.model.ts` (`addressLine`) · `src/server/services/auth.service.ts` (تصدير `issueSession` + `addressLine` في الـDTO) · `src/proxy.ts` (استثناء المعالج + عزل دور) · `src/components/common/status-badge.tsx` · `src/lib/query-keys.ts` · `src/app/provider/pending-review/page.tsx` (بُنيت بالكامل) · `tests/unit/route-guard.test.ts`

### ملف محذوف

`src/app/register/provider/documents/page.tsx` — الشاشة المستقلة من Phase 4 (`?professionId=`) اندمجت في المعالج كما نصّ البند المؤجَّل رقم 3 في `PHASE_04_REVIEW.md`.

---

## 3. Tests executed

```bash
npm run verify
```
(`typecheck` → `lint` → `check:constraints` → `test` → `build`)

## 4. Test results

| المجموعة | العدد | النتيجة |
|---|---|---|
| `tests/api/provider-registration.test.ts` | 38 | ✅ |
| `tests/unit/provider-registration.test.ts` | 35 | ✅ |
| `tests/unit/route-guard.test.ts` (+4 للمعالج) | 14 | ✅ |
| المجموعات السابقة (Phase 1–5) | 412 | ✅ |
| **الإجمالي** | **499** | **✅ 499/499** |

**تغطية ما طلبته الخطة نصًّا:**
- **كل خطوة:** مخططا الخطوتين 1 و2 مُختبَران حقلًا حقلًا (بريد إلزامي، تطابق كلمتي المرور، مدينة من قائمة الفيوم، عمر ≥ 18، عنوان 5–100 حرف، وصف 20–300، منطقة تغطية واحدة على الأقل، حدّا السعر عند `RANGE`، 6 مزايا كحد أقصى).
- **الرفض عند نقص مستند إلزامي حسب المهنة:** طلب الطبيب يُرفض بـ422 عند حذف الترخيص وحده، وطلب السبّاك يمرّ بمستنديه فقط.
- **منع استقبال الطلبات قبل الاعتماد:** `canAcceptOrders` تُفحص على مستوى الـAPI في كل الحالات، والحارس `assertCanAcceptOrders` مُختبَر على الحالات الخمس.
- **E2E:** تسجيل كامل ← مسودة (غير ظاهر) ← إرسال (غير ظاهر) ← اعتماد ← ظهور في نتائج البحث ← `canAcceptOrders = true`.

## 5. Build result

✅ `next build` ينجح — 46 مسارًا (7 مسارات API جديدة، 3 صفحات جديدة/مُعادة البناء).
`npm run check:constraints`: **166 ملفًا، صفر مخالفة**.

---

## 6. UI review — مقارنة بالصور 19–23

فُحصت الرحلة كاملة حيًّا على `375×812` ببيانات حقيقية.

| العنصر | الصورة | الحالة |
|---|---|---|
| Stepper أربع دوائر يُقرأ من اليمين، المنجز ✓ والنشط أزرق ممتلئ | 19–22 | ✅ |
| الخطوة 1: الاسم/الهاتف/البريد/كلمة المرور+تأكيدها · الموقع (محافظة + مركز + عنوان 0/100) · نوع الحساب + النوع + الميلاد · InfoAlert خصوصية | 19 | ✅ |
| الخطوة 2: التصنيف ← التخصص التابع له · سنوات الخبرة · وصف 0/300 · مناطق تغطية Multi-select · سعر تقريبي/لاحقًا · مزايا 0/200 | 20 | ✅ |
| الخطوة 3: بطاقات المستندات مبنيّة بالكامل من إعدادات المهنة | 21 | ✅ سبّاك: هوية + صورة شخصية (إلزاميان) + إثبات عنوان (اختياري) — بلا مؤهل ولا ترخيص |
| الخطوة 4: أربع بطاقات ملخّص بأزرار «تعديل» + Checkbox تعهّد + «إرسال طلب التسجيل» | 22 | ✅ الزر مُعطَّل حتى الإقرار — مؤكَّد حيًّا |
| شاشة 23: ✓ + حالة قيد المراجعة + رقم الطلب + تاريخ الإرسال + 24–48 ساعة + خطوات المراجعة الأربع + معلومات هامة | 23 | ✅ |
| شاشة الإدارة: طابور + تفاصيل + مستندات بروابط موقّتة + قرارات | مشتقّة | ✅ |
| الأرقام لاتينية داخل النص العربي (`SRV-2026-000001`, `80%`) | §1.2 | ✅ |
| Empty / Error / Loading لكل شاشة | §1.6 | ✅ |

**انحراف مقصود عن الصورة 23:** التصميم يذكر إخطارًا برسالة نصية؛ المنفَّذ إشعار داخل التطبيق + بريد، بلا SMS — وهو القرار المعتمد في `UI_ANALYSIS §2` والقاعدة العابرة «لا OTP».

---

## 7. Security review

| البند | الحالة |
|---|---|
| `verification.status` و`isVerifiedBadge` غير قابلين للتعديل إلا من ADMIN | ✅ مسار واحد فقط يكتبها (`decideVerification`) محروس بـ`requireRole('ADMIN')`. مخطط التعديل الذاتي **لا يحوي هذه الحقول أصلًا** و`.strict()` يردّ أي محاولة بـ400 — مُختبَر على 6 صور مختلفة للمحاولة. |
| مزوّد يحاول اعتماد نفسه | ✅ 403، والحالة تبقى `PENDING_REVIEW` — مُختبَر بالتأكد من عدم تغيّر السجل بعد المحاولة. |
| مستندات مزوّد غير مرئية لمزوّد آخر | ✅ من Phase 4 (404 لا 403)، وتفاصيل الإدارة تعيد **معرّفات** المستندات بلا روابط — مُختبَر نصيًّا بأن الاستجابة لا تحوي `res.cloudinary.com`. |
| الروابط الموقّتة | ✅ تُطلب لحظة فتح المستند ولا تُخزَّن في حالة الصفحة ولا في الـcache. |
| تسجيل كل قرار في `auditLogs` | ✅ مع الحالة **قبل وبعد** والفاعل والـIP — مؤكَّد حيًّا: سجلّان بفاعلين مختلفين (المزوّد أرسل، الإدارة اعتمدت). |
| بيانات الاتصال | ✅ الهاتف والبريد يخرجان في مسار الإدارة **وحده** (محروس بـADMIN)، ولا يظهران في أي مسار عام. |
| كلمة المرور في المسودة المحلية | ✅ **لا تُحفظ إطلاقًا** — مُستبعَدة صراحةً قبل الكتابة في `localStorage`، ومؤكَّد حيًّا أن المسودة المحفوظة لا تحوي كلمة المرور. |
| اكتمال المستندات | ✅ يُتحقق منه **على الخادم** عند الإرسال، لا بتعطيل زر الواجهة فقط — 422 مع تسمية كل مستند ناقص. |
| التعديل أثناء المراجعة | ✅ ممنوع (403) — الإدارة تراجع نسخة ثابتة. مسموح في `DRAFT` و`RESUBMISSION_REQUIRED` فقط. |
| الإرسال المكرّر | ✅ 409. |
| رقم الطلب | ✅ عدّاد ذرّي (`$inc` مع `upsert`) — لا يتصادم مع الفهرس الفريد عند تسجيلين متزامنين. |
| فشل كتابة سجل التدقيق | ✅ لا يُبطل قرارًا نُفِّذ فعلًا، ويُسجَّل بمستوى `error` للمراقبة. |

---

## 8. Issues found

### 🔴 (1) معالج التسجيل كان يطرد صاحبه فور إنشاء حسابه

`/register` مدرج في مسارات الضيوف، فأي مستخدم مسجّل يُحوَّل عنه. لكن هذا المعالج **يعبر حدّ المصادقة في منتصفه**: الحساب يُنشأ بعد الخطوة 2/4 ليصير رفع المستندات ممكنًا. بلا معالجة كان المزوّد يُقذف إلى `/provider/pending-review` لحظة نجاح التسجيل، فيستحيل الوصول للخطوتين 3 و4.

### 🟠 (2) إعادة تحميل الصفحة تُعيد المزوّد إلى الخطوة 1 وقد فقد بياناته

المسودة المحلية تُمسح عمدًا فور إنشاء الحساب (الخادم صار مصدر الحقيقة)، لكن المعالج لم يكن يقرأ حالته من الخادم — فمن يعيد التحميل في خطوة المستندات يجد نفسه في الخطوة 1 أمام نموذج فارغ. **اكتُشف بالتجريب الحيّ لا بالاختبارات.**

### 🟡 (3) «العنوان» فارغ في شاشة المراجعة بعد إعادة التحميل

`AuthUserDto` لم يكن يحمل `addressLine`، فبعد مسح المسودة لم يبقَ مصدر لعرضه في بطاقة الملخّص (الصورة 22).

### 🟡 (4) مخالفتا قاعدة React لتحديث الحالة داخل `useEffect`

مزامنة بيانات الخادم مع نموذج المعالج كانت في `useEffect`، وهو ما يسبّب دورات رسم متتالية وترفضه قاعدة `react-hooks/set-state-in-effect`.

### 🟡 (5) `DRAFT` كانت تكسر خريطة ألوان شارة التوثيق

إضافة الحالة تركت `Record<VerificationStatus, StatusTone>` ناقصة — أمسكها المُحقّق النوعي قبل التشغيل.

---

## 9. Issues fixed — كلها (5/5)

| # | الإصلاح | التحقق |
|---|---|---|
| 1 | استثناء صريح `GUEST_ONLY_EXCEPTIONS = ['/register/provider']`، مع إغلاقه أمام **العميل** المسجّل حتى لا ينشئ حسابًا ثانيًا من حسابه | 4 اختبارات حارس جديدة (زائر / مزوّد / عميل / بقية صفحات الضيوف) + المرور الحيّ الكامل عبر المعالج |
| 2 | استئناف من حالة الخادم: مستندات ناقصة ← الخطوة 3، مكتملة ولم تُرسل ← خطوة المراجعة | حيًّا: إعادة تحميل في منتصف المعالج تفتح «الخطوة 4 من 4 — مراجعة الطلب» بالبيانات كاملة |
| 3 | `addressLine` في `AuthUserDto` ومزامنته في المعالج | حيًّا: «العنوان: شارع الحرية، بجوار مسجد النور» بعد إعادة التحميل |
| 4 | نمط «تعديل الحالة أثناء الرسم» الموثّق في React بدل التأثير، وحارس مرجعي (`useRef`) لقراءة المسودة مرة واحدة | `npm run lint` نظيف بلا أي تعطيل للقاعدة |
| 5 | لون محايد لحالة `DRAFT` | `npm run typecheck` نظيف |

---

## 10. Remaining issues

| # | البند | الأثر | الجهة |
|---|---|---|---|
| 1 | رفع المستندات لم يُختبر حيًّا برفع حقيقي — لا بيانات اعتماد Cloudinary بعد | الرحلة الحيّة أُكملت بإدخال سجلات المستندات مباشرة في قاعدة التطوير؛ منطق الرفع نفسه مُغطّى بـ67 اختبارًا في Phase 4 | قبل الإطلاق (`npm run check:cloudinary`) |
| 2 | `assertCanAcceptOrders` مكتوبة ومُختبَرة لكن لا مسار طلبات يستدعيها بعد | المعيار مُتحقَّق منه على مستوى الـAPI عبر `canAcceptOrders` في `GET /provider/profile`؛ الاستدعاء الفعلي عند قبول الطلب | Phase 8 |
| 3 | `CLOUDINARY_AUTH_TOKEN_KEY` غير مضبوط — روابط المستندات موقّعة بلا انتهاء زمني | مورَّث من Phase 4 | قبل الإنتاج |
| 4 | لوحة الإدارة أوّلية: طابور + قرار فقط، بلا بحث ولا تصفية متقدمة ولا إدارة متطلبات المهن | مطابق لما نصّت عليه الخطة («شاشة إدارة أولية») | Phase 10 |
| 5 | مسودات `DRAFT` المهجورة تبقى بلا حدّ زمني | حسابات غير مكتملة تتراكم؛ تحتاج تنظيفًا دوريًا | Phase 10 |
| 6 | مقدم خدمة **مرفوض** لا يملك مسارًا لإعادة التقديم من الواجهة | القرار تصميمي: الرفض نهائي والتواصل عبر الدعم، بعكس «مطلوب إعادة إرسال» | قرار منتج |

**ملاحظة بيئة (لا خلل في المنتج):** تشغيلة واحدة للاختبارات أثناء عمل خادم التطوير وقاعدة بيانات المعاينة معًا أنهت 15 ملفًا من 17 (تخطّى ملفَي قاعدة بيانات لتجاوز مهلة إقلاع `mongod`). ثلاث تشغيلات متتالية بعد إغلاق المنافسة على الموارد أعطت **17/17 و499/499** في كل مرة.

---

## 11. Acceptance Criteria — التحقق

| # | المعيار | النتيجة |
|---|---|---|
| 1 | مزوّد `PENDING_REVIEW` لا يظهر في أي بحث — يُختبر على مستوى الـAPI | ✅ مُختبَر في `/providers` و`/search` قبل الإرسال وبعده وقبل القرار |
| 2 | مزوّد `PENDING_REVIEW` لا يمكنه قبول طلب — على مستوى الـAPI | ✅ `canAcceptOrders = false` في استجابة الـAPI + حارس مُختبَر على كل الحالات |
| 3 | مستندات مهنة السبّاك ≠ مستندات مهنة الطبيب | ✅ مُختبَر بالمقارنة الفعلية: الطبيب يحمل `PRACTICE_LICENSE` والسبّاك لا؛ ومؤكَّد بصريًا في الخطوة 3 |
| 4 | كل قرار مُسجَّل في `auditLogs` | ✅ بحالته قبل وبعد وفاعله — مؤكَّد حيًّا |
| 5 | توليد `SRV-YYYY-NNNNNN` | ✅ فريد لكل طلب بعدّاد ذرّي — `SRV-2026-000001` حيًّا |
| 6 | حساب `profileCompletion` | ✅ دالة خالصة بأوزان مُختبَرة (0 للفارغ، 100 للمكتمل، المستندات أثقل بند) — 60% ← 80% حيًّا بعد رفع المستندات |

**Definition of Done:** كل الميزات مكتملة ✅ · 499/499 اختبار ✅ · بناء نظيف ✅ · مقارنة بصرية بالصور 19–23 ✅ · مراجعة أمنية ✅ · دورة حياة كاملة مُختبَرة حيًّا (تسجيل → مسودة → إرسال → مراجعة إدارية → اعتماد → ظهور في البحث) ✅ · تقرير المراجعة ✅

---

## 12. تشغيل المراجعة محليًا

```bash
node scripts/dev-db.mjs
```

ثم في نافذة أخرى:

```bash
npm run db:seed -- --clear && npm run dev
```

حسابات المراجعة في قاعدة التطوير الحالية:
- **عميل:** `01011112222` / `Review12345`
- **إدارة:** `admin@khadamaty.local` / `AdminReview123`
- **مقدم خدمة معتمد:** `mostafa@example.com` / `Provider12345` (الطلب `SRV-2026-000001`)
