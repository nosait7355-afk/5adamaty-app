# PHASE_04_REVIEW.md — مراجعة المرحلة الرابعة

> **الحالة النهائية: ✅ مكتملة (COMPLETED)**
> التاريخ: 2026-09-03 · المرجع: `PROJECT_PLAN.md § Phase 4`

---

## 1. What was implemented

### 1.1 معمارية الرفع الآمن (ARCHITECTURE §8)
```
1) المتصفح → يقرأ أول 16 بايت من الملف + يطلب توقيعًا
2) السيرفر  → يتحقق من الدور + النوع الحقيقي (magic bytes) + الحجم
              → يوقّع معاملات محددة (مجلد + معرّف + نوع وصول + صيغ + حد أقصى)
3) المتصفح → يرفع مباشرة إلى Cloudinary بالتوقيع كما هو — الملف لا يمرّ عبر سيرفرنا
4) المتصفح → يرسل publicId فقط
5) السيرفر  → يستعلم Cloudinary نفسها عن الأصل الحقيقي، ويحفظ ما تقوله هي
              لا ما ادّعاه العميل، ثم يحفظ الـmetadata في MongoDB
```
**لا بايت صورة واحد يمرّ عبر سيرفر التطبيق أو يُخزَّن في MongoDB.**

### 1.2 فحص النوع الحقيقي (magic bytes)
`shared/lib/file-signature.ts` — يعمل في المتصفح والسيرفر معًا:
- تواقيع JPEG · PNG · WEBP (فحص مزدوج RIFF+WEBP) · PDF.
- **لا يثق بـ`file.type` ولا بالامتداد** — كلاهما من العميل وقابل للتزوير.
- قائمة حظر صريحة: **SVG** (ناقل XSS مخزَّن)، HTML، تنفيذيات، أرشيفات.
- عند التعارض بين المُعلن والحقيقي → رفض، لا تصحيح صامت.

### 1.3 Cloudinary (`server/lib/cloudinary.ts`)
- توقيع HMAC يدوي — بلا SDK رسمي، لتحكّم كامل فيما يُوقَّع بالضبط.
- **يثبّت في التوقيع**: `folder` (من هوية المستخدم لا الطلب) · `public_id` · `type` (`authenticated` للمستندات) · `allowed_formats` · `max_bytes` · `overwrite:false`.
- `fetchAssetDetails` يستعلم Cloudinary بعد الرفع — **مصدر الحقيقة الوحيد**، لا ادعاء العميل.
- `buildSignedAssetUrl` لروابط العرض الموقّتة (auth_token، صلاحية 5 دقائق عند توفر المفتاح).
- `deleteAsset` لتفادي الأصول اليتيمة.

### 1.4 خدمة الرفع والمستندات
- `upload.service.ts` — `issueUploadSignature` (تفويض حسب الدور والغرض) + `verifyAndBuildMediaRef` (تحقق ما بعد الرفع).
- `documents.service.ts` — **محرّك المستندات الديناميكي بالكامل من جانب السيرفر**: `saveDocument` يرفض أي `requirementKey` غير مذكور في `profession.documentRequirements` — حتى لو مرّرته الواجهة.
- `getDocumentUrl` يعيد **404 لا 403** لغير المالك (منع IDOR وتعداد المعرّفات).
- إعادة الرفع تستبدل السجل القائم عبر `findOneAndUpdate` ذرّي وتمسح أثر الرفض السابق.
- الحذف ممنوع بعد اعتماد الحساب إلا من الإدارة.

### 1.5 الواجهة
- `UploadCard` — 4 حالات (idle/uploading/done/error) مطابقة للصورة 21، مع شريط تقدّم وإعادة محاولة وحذف.
- `DocumentsStep` — **صفر قائمة مستندات ثابتة**؛ تُبنى بالكامل من `GET /professions/:id/document-requirements`.
- `FileHint` (من Phase 2) يُستخدم داخل زر الرفع بعد تصحيح التخطيط ليطابق التصميم.
- `upload-client.ts` — رفع مباشر بـ`XMLHttpRequest` (لدعم شريط التقدّم) + فحص محلي مسبق.

### 1.6 الـAPI (5 مسارات)
`POST /uploads/signature` · `GET/POST /provider/documents` · `GET /provider/documents/:id/url` · `DELETE /provider/documents/:id`

### 1.7 أدوات التشغيل
- `scripts/check-cloudinary.mjs` — يتحقق من صحة بيانات Cloudinary **فعليًا** عبر Ping API حقيقي، لا فحص وجود المتغيرات فقط.
- `env.ts` — Cloudinary إلزامي في الإنتاج + `CLOUDINARY_AUTH_TOKEN_KEY` اختياري مع تحذير عند غيابه.

---

## 2. Files changed

**جديد (17):**
```
src/shared/constants/uploads.ts
src/shared/lib/file-signature.ts
src/shared/schemas/upload.schema.ts
src/server/lib/cloudinary.ts
src/server/services/upload.service.ts · documents.service.ts
src/server/repositories/document.repository.ts
src/app/api/v1/uploads/signature/route.ts
src/app/api/v1/provider/documents/route.ts · [id]/route.ts · [id]/url/route.ts
src/lib/upload-client.ts
src/components/common/upload-card.tsx
src/components/features/provider/documents-step.tsx
src/app/register/provider/documents/page.tsx
scripts/check-cloudinary.mjs
tests/unit/uploads.test.ts · tests/api/documents.test.ts
```

**معدّل (4):** `env.ts` (متغيرات Cloudinary) · `.env.example` · `package.json` (سكربت `check:cloudinary`) · `vitest.config.mts` و`tests/helpers/db.ts` (إصلاح بيئي — القسم 7)

---

## 3. Tests executed & results

```
Test Files  11 passed (11)
     Tests  315 passed (315)
```

| الملف | العدد | التغطية |
|---|---|---|
| `unit/uploads.test.ts` | **44** | التعرّف على النوع الحقيقي (6) · التحقق الكامل (10) · توقيع Cloudinary (7) · **تفويض إصدار التوقيع (11)** · مخططات الرفع (7) · قواعد الرفع (5) |
| `api/documents.test.ts` | **23** | المحرّك الديناميكي (10) · قائمة المستندات (4) · **روابط موقّتة ومنع IDOR (7)** · الحذف (3) |
| باقي الملفات | 248 | Phase 1–3 |

### حالات أمنية مؤكَّدة بالاختبار
- SVG/HTML/EXE/ZIP متنكّرة بامتداد صورة → رُفضت جميعًا من محتواها لا من امتدادها.
- سبّاك لا يستطيع رفع `PRACTICE_LICENSE` ولا `PROFESSIONAL_CERT` حتى لو أرسلتهما الواجهة يدويًا.
- محاولة حفظ أصل خارج مجلد المستخدم (`documentPublicId(victimId)`) → مرفوضة.
- حجم حقيقي من Cloudinary يتجاوز الحد رغم ادعاء العميل بحجم أصغر → مرفوض.
- مستند حسّاس رُفع بوضع `upload` عام بدل `authenticated` → مرفوض.
- مزوّد آخر يطلب رابط مستند ليس له → **404** (لا 403).
- عميل عادي يطلب رابط مستند مزوّد → 404.
- الإدارة تصل لأي مستند.

### تحقق حيّ في المتصفح
- شاشة المستندات (3/4) اختُبرت لأربع مهن حقيقية من قاعدة بيانات فعلية: **سبّاك (3 مستندات) · كهربائي (3) · طبيب (5) · مدرّس خصوصي (4 بلا ترخيص)** — كلها من استعلام API حيّ.
- **رفع ملف SVG خبيث (`<script>alert(1)</script>`) بامتداد `.png` رُفض في المتصفح فورًا** بلا أي طلب شبكة — «تعذّر التعرّف على نوع الملف».
- زر «التالي» معطّل فعليًا حتى اكتمال المستندات الإلزامية، ويتحرّر تلقائيًا عند اكتمالها.
- صفر تجاوز أفقي عند 320px.

---

## 4. Build result

| الفحص | النتيجة |
|---|---|
| `tsc --noEmit` | ✅ PASS |
| `eslint --max-warnings=0` | ✅ PASS — 0/0 |
| `check:constraints` | ✅ PASS — **123 ملفًا، 0 مخالفة** |
| `vitest run` | ✅ PASS — 315/315 |
| `next build` | ✅ PASS — 27 مسارًا |
| `npm audit` | ✅ **0 vulnerabilities** |

---

## 5. UI review

مراجعة بصرية على شاشة المستندات (الصورة 21) عند 320px و375px، ببيانات حقيقية من 4 مهن:

| البند | النتيجة |
|---|---|
| بطاقة لكل مستند: أيقونة + عنوان + `*` + وصف يمينًا | ✅ |
| منطقة رفع منقّطة + قيود الملف **داخل** الزر | ✅ بعد إصلاح (القسم 7) |
| `*` الحمراء تظهر فقط للإلزامي، تختفي لإثبات العنوان | ✅ |
| ترتيب البطاقات من `order` في قاعدة البيانات | ✅ |
| صندوق «سيتم مراجعة مستنداتك خلال 24 ساعة» | ✅ |
| صندوق «مستندات إلزامية ناقصة» يتحدّث لحظيًا | ✅ |
| زر «التالي» معطّل حتى الاكتمال | ✅ مؤكّد تفاعليًا |
| Skeletons بأبعاد البطاقة الحقيقية | ✅ |

---

## 6. Security review

| البند | النتيجة | التحقق |
|---|---|---|
| السرّ لا يغادر السيرفر | ✅ | اختبار: التوقيع لا يحتوي `CLOUDINARY_API_SECRET` |
| فحص النوع من المحتوى لا الامتداد | ✅ | 4 حالات تنكّر مرفوضة |
| SVG محظور صراحةً | ✅ | ثنائيًا (رفض قبل حتى فحص التوقيع) |
| الحجم يُفحص مرتين (طلب + تحقق بعدي) | ✅ | حالة اختبار مخصّصة لكل مرحلة |
| المجلد من هوية المستخدم لا الطلب | ✅ | يمنع الرفع داخل مجلد غيرك |
| المستندات الحسّاسة `authenticated` إجباريًا | ✅ | يُرفض إن كانت `upload` |
| المحرّك الديناميكي مفروض على السيرفر | ✅ | ليس على الواجهة فقط |
| منع IDOR على المستندات | ✅ | 404 لا 403، 3 اختبارات |
| منع الخروج من المجلد في المعرّفات | ✅ | `publicIdSchema` يرفض `../` |
| حذف الأصل مع السجل | ✅ | يمنع الملفات اليتيمة |
| Rate limit مخصّص للرفع | ✅ | 20/دقيقة |

---

## 7. Issues found → Issues fixed

### 🔴 (1) بيئة الاختبار: قرص C ممتلئ (1GB متاح) — 141 اختبارًا فاشلًا
`mongodb-memory-server` يكتب ملفاته المؤقتة افتراضيًا في `os.tmpdir()` (قرص C على ويندوز)، والخطأ الظاهر («available disk space… less than required») بدا كعطل في الكود لكنه بيئي بحت — قرص المشروع (G) يملك 46GB متاحة.
**الإصلاح:** توجيه `dbPath` صراحةً داخل `.tmp-test-db/` بمشروع المشروع (قرص G)، مع تنظيف تلقائي بعد كل مجموعة اختبارات. أُضيف للـ`.gitignore`.
**أثر جانبي مقبول:** الكتابة على قرص حقيقي أبطأ من tmpfs الافتراضي (~10s بدل ~2s لكل ملف)، فرفعت `hookTimeout` إلى 90 ثانية وعطّلت التوازي بين ملفات اختبار قاعدة البيانات (`fileParallelism: false`) — التشغيل المتوازي كان يُحمّل القرص بعدة عمليات `mongod` معًا فيتجاوز المهلة.

### 🟠 (2) تخطيط شاشة المستندات لا يطابق التصميم — رصدته المراجعة البصرية
قيود الملف («JPG, PNG حتى 5MB») كانت تُعرض تحت الوصف النصي، بينما الصورة 21 تضعها **داخل** منطقة الرفع المنقّطة.
**الإصلاح:** نقل `FileHint` إلى داخل زر الرفع في كل الحالات الأربع (idle/uploading/done/error)، وتضييق عمود الرفع (`118px` تحت 400px، `150px` فوقها) ليمنع تكسّر النص.
**التحقق:** لقطات عند 320 و375px تطابق التصميم بلا تجاوز أفقي.

### 🟡 (3) محاكاة `Response` في اختبارات Cloudinary — 5 اختبارات فاشلة
`vi.spyOn(fetch).mockResolvedValue(response)` تعيد **نفس كائن** `Response` لكل استدعاء، وجسمه يُقرأ مرة واحدة فقط — فأي اختبار يحفظ أكثر من مستند واحد كان يفشل بـ«Body is unusable».
**الإصلاح:** `mockImplementation` تُنشئ `Response` جديدًا في كل استدعاء.

---

## 8. Remaining issues

### ⚠️ (1) لا توجد بيانات اعتماد Cloudinary حقيقية بعد
كل الاختبارات (44+23) تعمل بمحاكاة كاملة لاستجابات Cloudinary — **التحقق المنطقي مكتمل 100%**، لكن **لم يُختبر رفع حقيقي** إلى حساب Cloudinary فعلي بعد.
**للتفعيل:** ضع `CLOUDINARY_CLOUD_NAME` و`CLOUDINARY_API_KEY` و`CLOUDINARY_API_SECRET` في `.env.local` ثم:
```bash
npm run check:cloudinary   # يتحقق من صحة البيانات بـPing API حقيقي
```
لا حاجز إطلاق هنا بعد — Phase 6 (تسجيل مقدم الخدمة) هي أول من تحتاج رفعًا حقيقيًا فعليًا للعمل E2E.

### (2) بنود مؤجّلة
| # | البند | الجهة |
|---|---|---|
| 1 | `CLOUDINARY_AUTH_TOKEN_KEY` غير مضبوط في أي بيئة بعد — الروابط الموقّعة بلا انتهاء زمني حتى ضبطه | قبل الإنتاج |
| 2 | `saveDocument`/`removeDocument` غير مربوطتين بـAdmin verification workflow | Phase 6 |
| 3 | شاشة المستندات مستقلة الآن (`?professionId=`) — تُدمج في معالج التسجيل الكامل | Phase 6 |
| 4 | لا يوجد سكربت تنظيف دوري للأصول اليتيمة (رُفعت ولم تُحفظ metadata) | Phase 10 |

---

## 9. Acceptance Criteria — التحقق

| # | المعيار | النتيجة |
|---|---|---|
| 1 | لا ملف يمرّ عبر سيرفر التطبيق | ✅ رفع مباشر بمعاملات موقّعة |
| 2 | لا Base64 في MongoDB | ✅ metadata فقط (`MediaRef`) |
| 3 | تغيير إعدادات المهنة يغيّر الشاشة فورًا بلا نشر كود | ✅ مؤكّد حيًّا لأربع مهن |
| 4 | صفر قائمة مستندات ثابتة في الكود | ✅ `DocumentsStep` تُبنى من الـAPI بالكامل |
| 5 | التحقق من اكتمال المستندات على السيرفر لا الواجهة فقط | ✅ `saveDocument` يرفض المستند غير المطلوب |

**Definition of Done:** كل الميزات مكتملة ✅ · 315/315 اختبار ✅ · بناء نظيف ✅ · مراجعة UI مقابل الصورة 21 ✅ · مراجعة أمنية شاملة ✅ · تحقق حيّ يشمل رفض ملف خبيث فعليًا ✅ · تقرير المراجعة ✅
